# Binding Arc, Phase 3a — Compiler and Headers Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Teach the `.tmc` compiler to PRODUCE the symbolic forms phases 1 and 2
taught the object format and the linker to carry, and to CONSUME declarations
that arrive as source — a new `.tmh` header kind, sibling `.tmc` files, and the
interface sections of library objects. Two "not yet lowered" error codes are
retired by building what they stood in for: a bound call into an external
callee, and a routine that hands control to its own `state` parameter.

**Architecture:** One data path, opened at both ends. *Outward:* the compiler
always writes an interface section (`.param` lines, exported alphabets, the
exit count and the `returns` bit) and emits a symbolic bound site when the
callee is external — parameter names instead of positions, glyph labels instead
of indices — which the phase-2 pre-pass already resolves. *Inward:* a
declarations table, assembled from `--extern` files, sibling sources, library
objects and the embedded `std.tmh`, reaches `resolve_program` through a widened
`ExternalContracts`, so alphabet imports, named maps, library graph grafts and
early binding checks all read one source of truth. The IR gains the fields both
directions need in a single versioned encoding change (v3 → v4). Nothing in
`crates/core` moves except one link warning that is the linker half of a
compiler-side promise.

**Tech Stack:** Rust (pinned toolchain in `rust-toolchain.toml`), `proptest` for
the IR round trip, `cargo nextest run --workspace` as the final gate, no new
dependencies.

**Spec:** `docs/superpowers/specs/2026-09-13-issue-95-binding-analysis.md` —
"Item 1", "Item 2", the `state`-parameter half of "Item 3", "### Predicates"
(P6, P7, P9), the "**Compiler.**", "**Lint.**" and "**CLI.**" paragraphs of
"### By layer", and phase 3's line under "### Versions, sequencing, gates".

**Predecessors:**
`docs/superpowers/plans/2026-09-13-binding-arc-phase-1-format-and-assembler.md`
and `docs/superpowers/plans/2026-09-14-binding-arc-phase-2-linker.md` — both
landed on master at `e25e91d`; **the code is the authority now**, and where
either plan and the tree disagree the tree wins.

**Successor:** plan 3b, written after 3a merges, takes the whole "### Folded in
from #121" section (`enters`/`leaves`, `trap #2`, `--strip-asserts`,
`--release`, `tmt.json` 0.3, `enters-unmet`, `set`, `unused-set`,
`set-outside-alphabet`/`range-outside-alphabet`, the `char-arithmetic` fix, the
compiler-side `opaque` inference). Where a task here leaves a seam for 3b, it
says so.

**Reference map gathered for this plan:** `.superpowers/compiler-map-2026-09-15.md`
(file:line for everything below; verify before editing).
**Fixture verification log:** `.superpowers/plan3a-fixture-check.md` (every
command, exit code and first line of output behind a `[tool-verified]` mark).

---

## Rulings carried in (do not re-open)

From the controller's brief, ratified 2026-09-15:

- **R-3a-1 Two plans.** 3a is the spec's phase-3 line plus the phase-2 carries.
  3b is the #121 section, planned only after 3a merges. Nothing from phase 4
  (`outline` multi-exit, the LSP overlay, the stdlib rewrite, `docs/lsp.md`,
  CHANGELOG, release 0.6) enters either.
- **R-3a-2 Version literals.** `TMC_LANG_VERSION` (`parser.rs:34`) moves
  `0.1` → `0.2` in the FIRST task that changes the grammar (Task 5) and stays
  `0.2` through 3b — one unreleased bump per release. `TM_IR_VERSION`
  (`ir.rs:65`) moves `3` → `4` in Task 2, the one task that changes the IR
  encoding. Crates stay `0.5.x`; the 0.6 bump is phase 4.
- **R-3a-3 The embedded stdlib.** `std.tmc` text changes are phase 4. 3a adds
  `std.tmh` as **derived output**: pinned by a test to `tmt interface std.tmc`
  and regenerated by an `#[ignore]`d regen test like the goldens — never
  hand-edited. A task needing a declaration `std.tmc` does not carry uses a
  test fixture, not a stdlib edit. **Compiled-stdlib byte identity at both opt
  levels is a gate on every task that touches codegen or the compiler.**
- **R-3a-4 `.tmh` and the editors.** LSP routing of `.tmh` and every editor
  surface is phase 4. In 3a a `.tmh` is read by `compile --extern` and `build`,
  and produced by `tmt interface`. The TextMate grammars carry bidirectional
  drift guards (`crates/turing-machine/tests/editor_grammar.rs`), so a new
  keyword DOES enter `editors/grammars/tmc.tmLanguage.json` in the task that
  adds it — that is a drift guard, not an editor feature.
- **R-3a-5 `--extern` on `compile` only, `build` derives.** `--extern FILE`
  (repeatable, `.tmh` or `.tmc`) on `compile`; `build` derives declarations
  from sibling sources and from library objects' interface sections with no
  manifest key; header-only libraries are allowed. `--nostdlib` on `compile`
  means "do not read the embedded `std.tmh`".
- **R-3a-6 Behaviour-change honesty.** Every observable change — messages, exit
  codes, new output, retired codes, and the one shape a real user can hit —
  is named in "Behaviour changes in phase 3a", in prose a release note can quote.
- **R-3a-7 Docs are a task, not a footnote.** Task 17 is the documentation task
  and it lists its claims; every claim is checked against the code by the
  implementer.

Ratified 2026-09-13 with the arc and still binding:

- Qualified alphabet references; `writes` contracts in the interface section;
  `graph` is graft-only and graph bodies travel in `.tmh`; routines get `state`
  parameters; facades stay; no implicit routine form of a graph; cross-unit
  graph reuse is either a graft from the header or a call to a routine;
  `index-identity-map` stays opt-in; 5a kept; the `duplicate-graft-instance`
  lint; partial binding deferred with the path open; named maps in the arc;
  graph bodies verified by digest; `noreturn` inferred from the body and
  exported, `then` optional only against a known `noreturn` callee.

Carried from phase 2's own rulings (its plan and its final review):

- **Ruling 25** — an exit-count mismatch on a plain site reuses
  `LinkError::BadBinding` with the pre-pass wording; no new variant.
- **Ruling 29** (upheld) — no linker refusal for a tail-position framed call;
  the reviewer's `tail-call-no-continuation` warning is a phase-3 candidate,
  and this plan takes it (Task 16).
- **Ruling 30** — the fold scope is a callee reached through a *framed* call.
- **Ruling 33** — the tail refusal is gated on the BODY (`callee_can_return`),
  not on the `returns` bit alone, because the assembler does not enforce
  `noreturn`. Task 16 reuses that exact predicate.
- **A labelled pair carries `dst` 0** on the wire, and `open ⇒ map_written`.
  The compiler's emission must respect both (Task 10).

---

## Global Constraints

**Every task's requirements implicitly include this section. Re-read it before
each task.**

- **PM-1 byte identity.** No byte of any `pmt` output may change. This is a
  TM-compiler phase, so the PM crate should not appear in any diff at all;
  the one shared surface is `crates/core`, and Task 16 is the only task that
  touches it. Re-check deliberately on any `crates/core` change:
  `cargo test -p mtc-post-machine --test golden_programs` and
  `cargo test -p mtc-post-machine --test asm_volatile`.
- **Core neutrality.** `crates/core` learns nothing about TM-1. **A new
  `crates/core` diff in a compiler phase is a design smell — justify each
  one.** Exactly one is justified in this plan (Task 16, an arch-agnostic link
  warning over fields already on the wire); Task 3's text-expressibility
  decision is explicitly steered away from a second one.
- **`-O0` bit identity and the `brk` barrier.** Every optimizer pass learns the
  new IR variants (Task 11), and `-O0` output stays byte-identical to plain
  codegen for every program that uses none of them.
  `crates/turing-machine/tests/opt_equivalence.rs` and its
  `brk_barrier_blocks_elimination` stay green throughout.
- **Compiled-stdlib byte identity at BOTH opt levels** is a gate on every task
  touching the compiler or codegen: compile `stdlib::SOURCE` before and after,
  byte-compare the object at `-O0` and `-O1`. Note what CLAUDE.md says about
  this gate: for a FORMATTER change it is only a negative control. Here it is
  the real thing — the compiler IS in the path — with one exception, Task 3,
  which deliberately changes the object (it starts emitting an interface) and
  therefore states its expected delta instead of asserting identity.
- **The everything-matrix, `mode_equivalence`, the three-mechanism `.tma`
  matrix and the composition law tests stay green**
  (`crates/turing-machine/tests/opt_equivalence.rs::everything_matrix_is_green`,
  `tests/mode_equivalence.rs`, `tests/link_matrix.rs`,
  `crates/core/src/linker/compose.rs`'s law tests).
- **Drift guards are set-compares in BOTH directions, and every one of them is
  named in the task that trips it**: `error_code_docs.rs` (TM and core),
  `cli_docs.rs` (byte-compares usage blocks against `docs/tmt/cli.md`),
  `completions_registry.rs` + `EXPECTED_TOP_LEVEL` (the 15th subcommand),
  `man_page.rs`, `editor_grammar.rs`, the reserved-word count
  (`lexer.rs:666`), the `recognized_directives` guard against
  `docs/formats.md`, the stdlib twins/goldens, `tmc_golden.rs`, the IR
  round-trip test.
- **Docs policy.** Published pages (`README.md`, `CHANGELOG.md`, `docs/**`) and
  code comments cite `docs/<page>.md (keyword)` only — no `spec §N`, no
  `Task N`, no issue/PR numbers, no hosting URLs. **Forward citations are
  accepted within this phase**: a comment may cite
  `docs/tmt/language.md (headers)` before Task 17 lands that section. **The
  final whole-branch review checks that every citation resolves to a real
  page-plus-keyword** — one still dangling at review time is a defect.
  This plan is an internal artifact and may cite freely.
- **Commits.** Conventional with scope (`feat(turing-machine):`,
  `fix(turing-machine):`, `test(turing-machine):`, `feat(cli):`,
  `docs(turing-machine):`, `feat(core):`). Implementer subagents MAY commit
  their own task on branch `binding-arc-3`; merging and pushing stay the
  owner's. **Commit messages carry no Claude attribution and no
  `Claude-Session:` line** — the harness appends one, so every commit step ends
  with `git log -1 --format=%B` and, if a trailer was injected,
  `git commit --amend -F <file>` with the message stripped back. Messages are
  written to a file and passed with `-S -F`.
- **`git add` explicit paths, never `-A` or `.`** — a task commit carries its
  own files only.
- **Temp paths in tests**: PID plus a per-call atomic counter, never a fixed
  name. Copy `fn scratch` from
  `crates/turing-machine/tests/mode_equivalence.rs` (~`:935`) verbatim into any
  new TM test file that writes to disk.
- **Every fixture is run through the real tool before it is trusted.** Fixtures
  marked **[tool-verified]** in this plan were run, with the command, exit code
  and first output line recorded in `.superpowers/plan3a-fixture-check.md`.
  Fixtures marked **[shape-derived: …]** name what they were derived from and
  what the implementer must run to confirm them — always as **step 1** of the
  task that owns them. A fixture that does not assemble or compile is a plan
  defect; report it, do not paper over it.
- **Every test states, in one line, the mutation it catches**, and the fixture
  must FAIL if that mutation is applied. A fixture that merely parses and
  passes proves nothing. **For each new error or lint code there is a fixture
  that fires it and a near-miss fixture that does not.**
- **Cargo invocations** are prefixed
  `CARGO_TARGET_DIR=/Users/mellonis/Developer/mellonis-workspace/machines/toolchains/target`
  and run from the worktree root, one at a time, in the foreground. The final
  gate is `cargo nextest run --workspace` (never `cargo test` alone — one
  process per test is what exposed the stdlib `OnceLock` parse in phase 2),
  plus `cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D
  warnings`, `cargo build -p mtc-core --no-default-features` and
  `cargo build --workspace --lib --target wasm32-unknown-unknown`.

---

## Established facts this plan builds on

Verified against the working tree at `e25e91d` on 2026-09-15. An implementer
may rely on these without re-deriving them; each names its evidence.

1. **`.tmc` glyph literals decode multi-scalar clusters; `.tma` glyph literals
   refuse them.** `crates/turing-machine/src/lexer.rs:701-710`
   (`glyph_zwj_sequence_is_one_glyph`) pins a ZWJ sequence as ONE `.tmc` glyph;
   `crates/core/src/asm/lexer.rs:954` (`glyph_rejects_multi_char_content`) pins
   the refusal. Run end to end: a `.param` carrying `'👨‍👩‍👧'` is
   `error: not assembly text [raw-line]`, while the same file with `'🙂'`
   assembles and dis→asm round-trips byte-identically (log rows E2–E4). **The
   collision is dormant only because the compiler emits no `.param` today**;
   Task 3 wakes it.
2. **The compiler emits NO `.param` and no interface section** (phase-2
   established fact 3, re-verified: `tmt compile zwj.tmc -S` prints `.routine`
   lines and no `.param`, log row E1). So every glyph-level linker check
   landed in phase 2 is structurally dormant on the shipped corpus until
   Task 3.
3. **The shipped corpus is glyph-equal, not merely cardinality-equal.**
   `docs/examples/rpn/rpn.tmc:52` `alphabet bin { '_', '^', '$', '0', '1' }` is
   position-identical to `crates/turing-machine/src/stdlib/std.tmc:53`
   `export alphabet symbols { '_', '^', '$', '0', '1' }`. So Task 3's emission
   raises no `glyph-mismatch` on the four shipped plain `call std::` sites
   (log row G). This is a prior, not a substitute for Task 3's own measurement.
4. **`IrTape` does not carry glyph labels.** `ir.rs:112-120` is `{ name,
   alphabet, cardinality, volatile }` — `alphabet` is a NAME, documented as
   "presentation only". `codegen::emit_program`'s only input is `IrProgram`
   (`codegen.rs:158`). So `.param`'s glyph lists cannot be emitted without an
   IR addition; that is why Task 2 exists and why it is bigger than the spec's
   four-variant list.
5. **`ExternalContracts::modules()` returns `Vec<&'static Resolved>`**
   (`compiler.rs:2121-2128`), `'static` only because the stdlib is an
   `OnceLock`. Header declarations are per-invocation owned data, so the enum
   must become owned declarations with borrowed views — Task 1.
6. **`parse_reuse` always demands a `{ … }` body** (`parser.rs:1443-1460`), and
   that is the WHOLE header grammar delta: a `.tmh` carrying `export alphabet`,
   `namespace`, a `?` doc line, a signature and a `writes` clause fails at
   `expected '{' to open the body, found ';'` — column 61 of line 4, i.e. past
   everything else (log row C1). **[tool-verified]**
7. **`map` is already a reserved word** (`lexer.rs:35`, in `RESERVED[27]`), and
   a top-level `map` declaration fails with
   `expected a top-level declaration, found 'map'` (log row C2). So named maps
   cost a top-level arm, not a reserved-word bump. **[tool-verified]**
8. **The symbolic `.tma` the compiler must emit assembles, links and
   round-trips today.** The spec's own sketch — `mylib.tma` with `.param num,
   ('_','0','1'), writes=('0','1')` and `app.tma` with
   `call mylib::plusOne [num: 1{3->'0', 4->'1'}]` — assembles both sides, links
   with `1 composite(s), 0 stamp(s), 4 B compose table`, and disassembles back
   to the same operand (log rows D1–D4). **[tool-verified]** So Task 10 has a
   fixed target, not a guess.
9. **`exits=(…)`, `noreturn` and `retx #k` assemble and link today**
   (log rows D5–D6): `.routine sub, tapes=1, alpha=(3), exits=2, noreturn` with
   `call sub [t: 0] exits=(hit, miss)` links under `--call-mech mono` as
   `1 stamp(s)`. **[tool-verified]** Task 11's emission target is likewise fixed.
10. **The expander already dedups identical graft sites, and lint says nothing.**
    `expand.rs:1310-1314` keys an instance by
    `target ++ 0 ++ composite.key() ++ 0 ++ cont_key(cont)` and aliases on a
    hit (`:1321-1327`); two identical sites emit one subgraph and `tmt lint`
    reports only the two unrelated `unused-*` findings (log rows F1–F2).
    **[tool-verified]** `duplicate-graft-instance` keys on exactly that tuple,
    so it has no false positives by construction.
11. **The phase-2 predicate `tail-call-no-continuation` needs already exists.**
    `crates/core/src/linker/stamp.rs:1378-1389` (`check_splice_site`) computes
    `continuation(caller.blob.len(), addr).is_none() && callee_can_return(syntax,
    callee)`; `callee_can_return` (`:1339-1344`) reads the callee's `returns`
    bit and falls back to a body scan when the bit claims `noreturn`
    (ruling 33). The warning is that predicate applied to a `Plain`/`Bound`
    site. **It needs no header** — only the callee's `returns` bit and the
    caller's blob length, both already on the wire.
12. **`find_library` looks for `<name>.tmo` only** (`cli/build.rs:452-460`) and
    errors "`library X not found on the -L search path`" otherwise. Header-only
    libraries (Task 15) widen exactly this function.
13. **The interface section's `enters`, `leaves` and `opaque` fields already
    exist on the wire** (`crates/core/src/formats/object/mod.rs:239-250`,
    landed by phase 1). 3a writes `None`/`false` into all three and leaves them
    as 3b's seam; the reader already rejects a present-but-empty `enters`/
    `leaves` list as `Malformed`.
14. **`Interface.imports` is dormant** (`mod.rs:261-270`; the linker's
    `AlphabetDrift` check is live but no writer fills the list). Task 8 fills it.
15. **`ExportedGraph.digest` is on the wire and nothing computes it.**
    `docs/formats.md:253,369,684` specify the field and the `.graph`/`.grafted`
    directives; no code produces a canonical-text CRC-32. Task 4's printer
    becomes that text's single source of truth, which is why Task 13 depends
    on Task 4.

---

## File structure

| File | Responsibility in this phase |
|---|---|
| `crates/turing-machine/src/compiler.rs` | `ExternalContracts` → owned `Declarations` (T1); qualified alphabet resolution and the `unresolved-alphabet` split (T8); named-map declaration checks (T9); the `undefined-graph` split (T13); the `noreturn` body fact and its check (T12) |
| `crates/turing-machine/src/declarations.rs` | **NEW (T1).** The declarations table — owned `Resolved` modules plus their provenance (embedded stdlib, `--extern` file, sibling source, library object) — and the borrowed view `resolve_program` reads |
| `crates/turing-machine/src/header.rs` | **NEW (T5).** Declarations-only reading: the `.tmh` entry point over the same green parse, and the canonical header printer `tmt interface` renders (T4) |
| `crates/turing-machine/src/parser.rs` | `TMC_LANG_VERSION` 0.1 → 0.2 (T5); the `;`-terminated signature alternative (T5); `map` as a top-level item and `export map` (T9); qualified alphabet names in `sig_param` and `tape` (T8); `noreturn` in a signature (T12); `state` args at a call site (T11) |
| `crates/turing-machine/src/lexer.rs` | `RESERVED` 27 → 28 (`noreturn`), count pin updated (T12) |
| `crates/turing-machine/src/syntax/{kinds,views,extract}.rs` | New node kinds for the map declaration and the bodiless signature; typed views and extraction for both (T5, T9) |
| `crates/turing-machine/src/ir.rs` | **IR v4** (T2): `IrTape.glyphs`/`.writes`, `IrWorld.exits`/`.returns`, `IrTransition::ReturnExit`, `CallThen.exits`, `IrTapeBinding.param`, `IrMapPair.dst` index-or-label, `TM_IR_VERSION` 3 → 4. Then `resolve_binding`'s symbolic branch (T10) and `retx` lowering (T11) |
| `crates/turing-machine/src/codegen.rs` | `.param` lines, `exits=`/`noreturn` on `.routine` (T3); named entries and glyph labels in `render_binding` (T10); the `exits=(…)` operand and `retx` (T11) |
| `crates/turing-machine/src/expand.rs` | Library graphs grafted from declarations, and the spliced body's digest recorded (T13); the `graft-call-unsupported` advice (T13) |
| `crates/turing-machine/src/optimizer/**` | Every pass learns `ReturnExit` and `CallThen.exits` (T11) |
| `crates/turing-machine/src/footprint.rs` | The new variants (T11); external callees' declared `writes` read from the declarations table rather than only the stdlib (T1) |
| `crates/turing-machine/src/stdlib/std.tmh` | **NEW (T7), derived output.** Never hand-edited |
| `crates/turing-machine/src/stdlib/mod.rs` | `header()` beside `object()`/`resolved()` (T7) |
| `crates/turing-machine/src/lint/rules/{unused_map,duplicate_graft_instance,unreachable_continuation}.rs` | **NEW** (T9, T14, T12) |
| `crates/turing-machine/src/lint/rules/dead_map_pair.rs` | Extended to named maps (T9) |
| `crates/turing-machine/src/cli/interface.rs` | **NEW (T4).** The 15th subcommand, source and object arms behind `sniff()` |
| `crates/turing-machine/src/cli/{mod,build,driver}.rs` | `interface` in both dispatch tables (T4); `--extern`/`--nostdlib` on `compile` (T6); `build` derives declarations and finds `<name>.tmh` (T15) |
| `crates/turing-machine/src/completions/registry.rs` | `interface_spec()`; `compile_spec()` gains `--extern` and `--nostdlib` (T4, T6) |
| `crates/core/src/linker/{mod,engine}.rs` | `tail-call-no-continuation` in `DIAGNOSTIC_CODES` and in `check_sites` (T16) — **the one `crates/core` diff** |
| `editors/grammars/tmc.tmLanguage.json` | `noreturn` in the keyword tier (T12) — a drift guard, not an editor feature |
| `crates/turing-machine/tests/interface_emission.rs` | **NEW (T3).** The interface section the compiler writes, and the corpus measurement |
| `crates/turing-machine/tests/header_roundtrip.rs` | **NEW (T4/T5).** `tmt interface` from source and from an object; declarations-only reading |
| `crates/turing-machine/tests/extern_declarations.rs` | **NEW (T6).** `--extern`, `--nostdlib`, a user library under `std` |
| `crates/turing-machine/tests/stdlib_header.rs` | **NEW (T7).** The `std.tmh` pin and its `#[ignore]`d regen |
| `crates/turing-machine/tests/cross_unit.rs` | **NEW (T8/T10).** Alphabet imports, qualified references, symbolic emission end to end |
| `crates/turing-machine/tests/named_maps.rs` | **NEW (T9).** Declaration checks, site checks, the two lints |
| `crates/turing-machine/tests/state_params.rs` | **NEW (T11).** `state` parameters through all three mechanisms |
| `crates/turing-machine/tests/library_grafts.rs` | **NEW (T13).** Grafts from a header, digests, drift |
| `crates/turing-machine/tests/plain_site_sweep.rs` | The `#[ignore]`d phase-2 instrument, re-run by Task 3 |
| `docs/tmt/language.md` | The 0.2 language reference: headers, `state` parameters, named maps, qualified alphabets, imports, `noreturn`, the moved same-glyphs rule (T17) |
| `docs/tmt/cli.md` | `interface`, `--extern`, `--nostdlib`, two retired code rows, the new rows (T17, and each surface's own task) |
| `docs/tmt/lint.md` | The new lints (T17) |
| `docs/tmt/project.md` | How `build` derives declarations; header-only libraries (T15, T17) |
| `docs/formats.md`, `docs/core.md` | Where the linker-side checks gain a compiler feeder (T17); the text-expressibility caveat count (T3) |
| `CLAUDE.md` | The version table, "fourteen" → "fifteen" subcommands, the standing-state paragraphs that say "until phase 3" (T17) |

---

### Task 1: Declarations become owned data (no behaviour change)

**Why first.** Everything the compiler learns from outside its own unit —
alphabets, maps, routine signatures, graph bodies, `writes` contracts — arrives
through one table. Today there is exactly one such source, the embedded stdlib,
and it is `&'static` because it lives in a `OnceLock` (Established fact 5).
Headers are per-invocation owned data. Widening the carrier BEFORE any consumer
exists keeps the refactor honest: it must change no behaviour at all.

**Requirements**

- `ExternalContracts` becomes a struct carrying owned declaration modules plus
  each one's provenance; the two old spellings survive as constructors so no
  call site's INTENT changes:
  - `Declarations::stdlib()` — today's `ExternalContracts::Stdlib`.
  - `Declarations::none()` — today's `ExternalContracts::None`, still what
    `stdlib::object()` and `stdlib::analysis()` pass to avoid recursing into
    their own `OnceLock`.
- The borrowed view `resolve_program`, `check_contracts` and
  `footprint::infer_resolved_with` read is a `&[&Resolved]` produced by one
  method, exactly as `modules()` produces one today.
- **Provenance is carried from the start**, even though nothing reads it until
  Task 13: each entry records whether it came from the embedded stdlib, an
  `--extern` file (with its path), a sibling source, or a library object. A
  diagnostic that has to say "declared by `use`, but its declarations were not
  given" (Task 8) and a digest check that has to name the library (Task 13)
  both need it, and retrofitting it would touch every construction site twice.
- `CompileOptions.externals` changes type. Every construction site follows:
  `compiler.rs`, `stdlib/mod.rs:57-73` and `:104-121`, `cli/build.rs:150`,
  `cli/driver.rs:426`, `:366`, `:716`, and **`crates/wasm/src/inner/`** — the
  browser binding compiles `.tmc` through this same path and carries a
  stdlib-object byte pin plus the `inner/`-is-free-of-`wasm-bindgen` test.
- `Declarations` is `Clone` but not `Copy` (it owns `Resolved`s), so
  `CompileOptions` loses `Copy` on that field. Check every `..Default::default()`
  and every by-value pass; prefer `&Declarations` in function signatures.

**TDD steps**

- [ ] **Step 1: The identity test, written first**

  In `crates/turing-machine/tests/interface_emission.rs` (new file, which
  Task 3 fills out):

  - `the_stdlib_object_is_byte_identical_at_both_opt_levels` — compile
    `stdlib::SOURCE` at `-O0` and at the release preset, serialize both objects,
    and compare against bytes captured before this task. **Mutation it catches:**
    swapping `Declarations::stdlib()` for `Declarations::none()` anywhere in the
    stdlib path changes the inferred footprint and therefore a contract check;
    the byte compare goes red.
  - `declarations_none_and_stdlib_differ_in_module_count` — a one-line
    structural pin so the two constructors cannot silently converge.
    **Mutation:** making `none()` return the stdlib module.

- [ ] **Step 2: Introduce `crates/turing-machine/src/declarations.rs`**

  ```rust
  /// Where one declaration module came from. Read by the diagnostics that
  /// must tell "no such name" from "its declarations were not given"
  /// (docs/tmt/cli.md (error codes)) and by the graft-digest check
  /// (docs/formats.md (routine interfaces)).
  #[derive(Debug, Clone, PartialEq, Eq)]
  pub enum Origin {
      /// The embedded standard library, read implicitly unless `--nostdlib`.
      Stdlib,
      /// A `--extern FILE` given on the command line.
      Extern(PathBuf),
      /// Another source of the same build target.
      Sibling(PathBuf),
      /// The interface section of a library object on the `-L` path.
      Library(String),
  }

  /// Everything the compiler knows about units other than its own
  /// (docs/tmt/language.md (declarations)). Owned, because a header is read
  /// per invocation; the embedded stdlib is cloned in from its `OnceLock`
  /// rather than borrowed, so one type serves both.
  #[derive(Debug, Clone, Default)]
  pub struct Declarations {
      modules: Vec<(Origin, Resolved)>,
  }

  impl Declarations {
      pub fn none() -> Self { Self::default() }
      pub fn stdlib() -> Self { … }           // clones `crate::stdlib::resolved()`
      pub fn push(&mut self, origin: Origin, resolved: Resolved) { … }
      /// The borrowed view every consumer reads — the exact shape
      /// `ExternalContracts::modules()` returned.
      pub fn modules(&self) -> Vec<&Resolved> { … }
      pub fn origin_of(&self, resolved: &Resolved) -> Option<&Origin> { … }
  }
  ```

  Cloning the stdlib's `Resolved` per compile is the deliberate cost: it keeps
  ONE type for both cases and removes the `'static` constraint that blocks
  headers. Measure it if a task later reports a slowdown; the phase-2 ledger
  records that `expand` dominates a keystroke, not resolution.

- [ ] **Step 3: Thread it**

  Replace `ExternalContracts` at every site in the reference map's compiler
  row. `analyze_with(source, &Declarations)`, `analyze_staged_with(source,
  &Declarations)`, `check_contracts(resolved, &Declarations)`,
  `footprint::infer_resolved_with(resolved, &decls.modules())`.

- [ ] **Step 4: The wasm arm**

  Update `crates/wasm/src/inner/` construction sites. Then run, in this order:
  `cargo build --workspace --lib --target wasm32-unknown-unknown`, and
  `cargo test -p mtc-wasm` (which holds the stdlib-object byte pin and the
  no-`wasm-bindgen`-in-`inner/` test).

- [ ] **Step 5: Gate**

  `cargo nextest run -p mtc-turing-machine`, then `-p mtc-wasm`, then
  `cargo clippy --workspace --all-targets -- -D warnings`.
  **This task must change no test's expected output anywhere.** A test that
  needed editing is a signal the refactor changed behaviour — stop and report.

- [ ] **Step 6: Commit** — `refactor(turing-machine): external declarations
  become owned data with provenance`. Then `git log -1 --format=%B`; amend if a
  trailer was injected.

---

### Task 2: IR v4 — the encoding

**Why here.** Two later tasks (10, 11) fill IR variants, and R-3a-2 puts the
`TM_IR_VERSION` bump in "the task that changes the IR encoding" — singular. So
the whole encoding lands once, with its round trip, before any consumer. This
is the shape phase 1 used for the object format.

**Requirements**

Six additions, three of them beyond the spec's own list — see "Decisions for
the controller" (D2) for why:

| Field | Type | Consumer |
|---|---|---|
| `IrTape.glyphs` | `Vec<String>`, len == `cardinality` | `.param` glyph lists (T3) |
| `IrTape.writes` | `Option<Vec<String>>` | `.param`'s `writes=(…)` (T3) |
| `IrWorld.exits` | `u8` | `.routine`'s `exits=` (T11) |
| `IrWorld.returns` | `bool` | `.routine`'s `noreturn` (T12) |
| `IrTransition::ReturnExit { exit: u32 }` | new variant | `retx #k` (T11) |
| `IrTransition::CallThen.exits` | `Vec<u32>` (state ids) | the `exits=(…)` operand (T11) |
| `IrTapeBinding.param` | `Option<String>` | a symbolic entry (T10) |
| `IrMapPair.dst` | `IrMapDst::{Index(u32), Label(String)}` | a glyph-labelled pair (T10) |

- `#[serde(default, skip_serializing_if = …)]` on every added field that has a
  natural empty value, so a v3 document with none of them deserializes into a
  v4 struct unchanged — **but the version field still moves to 4**, because the
  encoding's vocabulary grew. `IR_VERSION` is the acceptance contract, not a
  compatibility hint.
- `TM_IR_VERSION` `3` → `4` at `ir.rs:65`; the two stamping sites (`:473`,
  `:1155`) follow for free.
- **3b's seam, stated not built:** `IrTape` will gain `enters`, `leaves` and
  `opaque` in 3b. They are NOT added here — a field written as `None` by
  everything is not a seam, it is dead weight, and 3b bumps no version (R-3a-2
  keeps `TMC_LANG_VERSION` at 0.2 and says nothing about IR, so **3b will need
  its own `TM_IR_VERSION` decision; record it as a question, do not pre-empt
  it**).
- Nothing in this task PRODUCES a new variant. Lowering still refuses a
  `state`-parameter continuation and an external binding; the fields are filled
  with their empty values. This is what keeps the task independently reviewable.

**TDD steps**

- [ ] **Step 1: Round-trip tests first**

  In the existing IR round-trip test file (find it by
  `grep -rn "TM_IR_VERSION" crates/turing-machine/tests/`):

  - `v4_documents_round_trip` — construct an `IrProgram` exercising every new
    field (a labelled `dst`, a named `param`, a two-exit `CallThen`, a
    `ReturnExit`, glyphs and a `writes` list, `noreturn`), serialize,
    deserialize, assert equality. **Mutation:** dropping `param` from the
    `Serialize` derive; the compare goes red.
  - `a_v3_shaped_document_deserializes_with_empty_new_fields` — feed the JSON a
    v3 build wrote (take one from an existing golden, edit only its `version`),
    assert every new field is at its empty value. **Mutation:** removing
    `#[serde(default)]` from `IrTapeBinding.param`; deserialization fails.
  - `the_version_literal_is_four` — a bare assert against `TM_IR_VERSION`,
    with the comment naming what a version means here. **Mutation:** leaving
    it at 3.

- [ ] **Step 2: The `IrMapDst` enum**

  ```rust
  /// A map pair's destination: a callee symbol INDEX when the callee is in
  /// this compilation unit, or its glyph LABEL when it is not and the linker
  /// must resolve it against the callee's interface
  /// (docs/formats.md (bound calls)).
  #[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
  #[serde(untagged)]
  pub enum IrMapDst {
      Index(u32),
      Label(String),
  }
  ```

  `untagged` keeps a v3 document's bare `"dst": 3` readable and keeps the v4
  JSON legible — a label is a string, an index a number, and the two can never
  be confused. **Verify this against `serde`'s untagged rules before relying on
  it**; if a number ever deserializes as a `Label`, fall back to an externally
  tagged enum and say so in the task report.

- [ ] **Step 3: Add the fields; fix every construction site**

  `grep -n 'IrTape {' -r crates/turing-machine/src` and likewise for `IrWorld`,
  `IrTapeBinding`, `IrMapPair`. Fill glyphs and `writes` from the resolved
  alphabet and the tape's contract at lowering time — the data is already in
  `Expanded`/`Resolved` (`ir.rs`'s lowering reads `expanded.alphabets[…].glyphs`
  at `:780-798` today for `resolve_binding`), so this is a plumb, not an
  inference.

- [ ] **Step 4: `tmt ir` output**

  `cli/inspect.rs`'s `ir` subcommand writes the JSON artifact. Its golden files
  (if any — `grep -rn 'emit-ir' crates/turing-machine/tests/`) move by exactly
  the new fields and the version digit. Regenerate deliberately and inspect the
  diff: a golden moving in any OTHER field is the signal that the plumb in
  step 3 changed a value rather than adding one.

- [ ] **Step 5: Gate**

  `cargo nextest run -p mtc-turing-machine`. **Compiled-stdlib byte identity at
  both opt levels** — the object must not move, since codegen ignores every new
  field until Task 3. `cargo test -p mtc-turing-machine --test opt_equivalence`
  for `-O0` bit identity.

- [ ] **Step 6: Commit** — `feat(turing-machine): TM IR 4 carries glyphs,
  contracts, exits and symbolic binding entries`.

---

### Task 3: The compiler writes its interface — and the corpus measurement

**PULLABLE: no.** This is the spine. Tasks 10, 12 and 16 all read a callee's
interface, and until this lands there is none to read.

**Why the measurement is step 1.** Phase 2 landed glyph-level plain-site checks
that are structurally dormant because no compiled object carries an interface
(Established fact 2). This task wakes them on the shipped corpus. Established
fact 3 is the prior — `rpn.tmc`'s `bin` is position-identical to
`std::binaryNumbers::symbols`, so nothing should fire — but a prior is not a
measurement, and phase 2's own Task 1 set the precedent of recording the number.

**Requirements**

- Codegen emits, for every world, `.param <name>, (<glyphs>)[, writes=(<glyphs>)]`
  lines in tape order immediately after the `.routine` line, from `IrTape.glyphs`
  and `IrTape.writes` (Task 2). `writes=()` is never printed (phase-1 ruling);
  an absent `writes` clause prints no suffix.
- `exits=` and `noreturn` on `.routine` are NOT emitted here — `IrWorld.exits`
  is 0 and `returns` is `true` for everything until Tasks 11 and 12. Emitting
  a suffix for a zero value would break the "lowest version that carries the
  content" rule the writer follows.
- Exported alphabets go into `Interface.alphabets` — a compiler fact with no
  directive, printed by `tmt dis` as `; alphabet <name>: (<glyphs>)` (already
  implemented core-side).
- **The text-expressibility decision lands here** (brief carry 3). See
  Decisions (D1). **This plan's pick: declare the fourth exception**, because
  the alternative — widening `.tma`'s glyph literal to accept any quote-free
  content — drops a documented rule, flips a pinned core test
  (`glyph_rejects_multi_char_content`) and puts a `crates/core` diff in a
  compiler phase for a shape the corpus does not contain. Concretely:
  - `docs/formats.md`'s "Text-expressibility caveats" list goes from three
    bullets to four, the new one naming the multi-scalar `.tmc` glyph as a
    label the compiler CAN now emit and the assembler cannot spell (the
    existing third bullet describes the same fact from the format's side; the
    new one states that the compiler reaches it).
  - `CLAUDE.md:213`'s count is corrected in Task 17 — it currently says three
    and names only `-g` plus exported and imported alphabets, which already
    disagrees with `formats.md`'s own list.
  - The dis→asm byte-identity test gains an explicit exclusion with this
    reason, not a silent skip.

**TDD steps**

- [ ] **Step 1: MEASURE, before any edit**

  Run the phase-2 instrument and the two CLI sweeps, and record the numbers in
  the task report:

  ```
  cargo test -p mtc-turing-machine --test plain_site_sweep -- --ignored --nocapture
  ```

  Then, over `docs/examples/**` and the stdlib, capture `tmt build -v` and
  `tmt link -v` output BEFORE the change. Repeat both AFTER step 4 and diff.

  **The gate:** a `glyph-mismatch` or `narrow-alphabet` finding on a shipped
  example STOPS the task — report it to the controller rather than editing an
  example, because a shipped program that was silently re-labelling symbols is
  a finding about the corpus, not about this task. Established fact 3 says none
  should fire. **A sweep that resolves nothing is a failed sweep, not a clean
  one** — phase 2's own wording.

- [ ] **Step 2: Emission tests first**

  In `crates/turing-machine/tests/interface_emission.rs`:

  - `a_compiled_routine_carries_its_parameter_names_and_glyphs` — compile a
    two-tape routine, read `object.interface`, assert `params`, `glyphs` and
    `writes` field for field. **Mutation:** emitting glyphs in alphabet
    declaration order rather than band order; the assert on the second tape
    goes red. (Make the two tapes' alphabets DIFFER, or the mutation is
    invisible.)
  - `the_writes_suffix_is_absent_without_a_clause` and
    `the_writes_suffix_lists_only_the_declared_glyphs` — the pair.
    **Mutation:** defaulting an absent `writes` to the full alphabet, which is
    what `declared_effective` does internally and is the easy mistake; the
    first test goes red.
  - `an_exported_alphabet_reaches_the_interface_section` +
    `a_local_alphabet_does_not`. **Mutation:** dropping the `exported` filter.
  - `emitted_assembly_reassembles_to_the_same_object` — `compile -S`, then
    assemble the text, then byte-compare the two objects. **Mutation:** any
    `.param` spelling the assembler cannot read (this is the round-trip gate,
    and it is what would have caught the multi-scalar glyph had the corpus
    contained one).
  - `a_multi_scalar_glyph_label_is_the_declared_fourth_exception` — compile the
    ZWJ fixture (log row E1's `zwj.tmc`), assert the object's interface
    carries the multi-scalar label, assert `compile -S`'s text does NOT
    reassemble, and assert the failure is the assembler's `raw-line` error at
    the `.param` line. **Mutation:** if someone later widens the `.tma` glyph
    literal, this test goes red and forces the caveat list to be revisited
    deliberately. **[tool-verified]** — log rows E1–E4.

- [ ] **Step 3: Emit**

  `codegen.rs::emit_func` (`:734-748`) gains the `.param` loop. The glyph-list
  spelling is `docs/formats.md (canonical spelling)`: one space after the
  directive word, `, ` between fields, ranges NOT collapsed (the compiler holds
  data, not text).

- [ ] **Step 4: Re-measure** (step 1's second half) and record the delta.

- [ ] **Step 5: Gate**

  Full `cargo nextest run --workspace`. **This is the one task where
  compiled-stdlib byte identity is EXPECTED to break** — the stdlib object now
  carries an interface section. So: record the object's new size and its
  interface content, assert the CODE blobs are byte-identical to before (a
  helper that compares `object.blobs` alone), and re-pin the byte gate to the
  new object. **Assert the code-blob identity explicitly** — without it, a
  codegen regression hides behind "the object was supposed to change".
  `crates/wasm`'s stdlib-object pin moves the same way; update it in this task.

- [ ] **Step 6: Commit** — `feat(turing-machine): every compiled object
  describes its interface`, with the measurement in the body.

---

### Task 4: `tmt interface` — the fifteenth subcommand

**Requirements**

- `tmt interface INPUT [-o OUT.tmh]`. `INPUT` is a `.tmc` or a `.tmo`, told
  apart by `mtc_core::formats::sniff()` on the container magic
  (`crates/core/src/formats/mod.rs:83`), **never by extension** — the
  `tape-block new --from` precedent, and a standing rule of this repo.
- **Two arms over one printer.** From source the header is complete: exported
  alphabets, exported maps (Task 9 fills this in; until then the printer emits
  none and the header is still valid), `export routine` signatures with their
  contracts and `?` doc lines, and `export graph` bodies **in full**. From an
  object it carries routine signatures, contracts and exported alphabets but no
  graphs, maps or doc lines — the one difference worth stating in `--help`.
- The printer lives in `crates/turing-machine/src/header.rs` (created here,
  extended by Task 5's reader). It is the **canonical rendering**, which makes
  it the single source of truth for the graph digests Task 13 computes — so it
  must be deterministic and independent of the input's whitespace and comments.
  State that obligation in the module doc.
- Output goes to stdout without `-o`, to the file with it. Errors are typed and
  rendered in `cli/`, per the thin-renderer rule.

**Drift guards this task trips — all four must be updated in it:**
`completions/registry.rs` (`interface_spec()`), `EXPECTED_TOP_LEVEL` in
`crates/turing-machine/tests/completions_registry.rs:90-105` (14 → 15 names),
`crates/turing-machine/tests/man_page.rs` (the man module renders from
`cli::usage_text`, so a subcommand missing from that table fails its tests),
and `crates/turing-machine/tests/cli_docs.rs` (which byte-compares the usage
block against `docs/tmt/cli.md` — so `docs/tmt/cli.md` gains the `interface`
section **in this task**, not in Task 17).

**Fixtures**

`[shape-derived: `std.tmc`'s declarations plus the `.param` canonical spelling
at `docs/formats.md:705-715`]` — the subcommand does not exist, so the expected
text cannot be tool-verified. **Step 1 of this task is to generate it and read
it**, not to match it against a transcription. The plan's expected shape, for
`mylib.tmc` (the spec's own sketch):

```
export alphabet bits { '_', '0', '1' }
export alphabet wide { '_', 'a', 'b', '0', '1' }
namespace mylib {
  ? Add one to the binary number under the head.
  export routine plusOne(tape num: bits writes { '0', '1' });
}
```

This is the same text log row C1 fed to the compiler, which parsed everything
except the `;` — so it is a header the Task 5 reader will accept by
construction.

**TDD steps**

- [ ] **Step 1: Generate and read.** Build the printer far enough to run
  `tmt interface crates/turing-machine/src/stdlib/std.tmc` and **read the
  output by eye** before writing any expectation. Record it in the task report.
  Two things to look for: a declaration the printer drops, and any
  non-determinism (iteration over a `HashMap`).

- [ ] **Step 2: Tests**

  In `crates/turing-machine/tests/header_roundtrip.rs`:

  - `the_source_arm_prints_every_exported_declaration` — over a fixture with an
    exported and a local alphabet, an exported and a local routine, and an
    exported graph. **Mutation:** printing local declarations too; the
    negative half goes red.
  - `the_object_arm_prints_signatures_and_alphabets_but_no_graphs` — assemble
    the same unit, run the object arm, assert the graph section is absent and
    the routine signatures are present. **Mutation:** falling through to the
    source arm; the graph reappears.
  - `sniff_not_extension_decides_the_arm` — feed a `.tmo` **renamed to
    `.tmc`** and assert the object arm ran. **Mutation:** dispatching on the
    extension. This is the test that pins the repo's standing rule.
  - `the_printer_is_insensitive_to_comments_and_whitespace` — two sources
    differing only in comments and indentation print byte-identical headers.
    **Mutation:** any printer path that echoes source text rather than
    rendering from the resolved module. **This is the digest's precondition**
    and Task 13 depends on it.
  - `interface_output_reparses_as_a_header` — `#[ignore]`d until Task 5 lands
    the reader, then un-ignored **by Task 5**. Note the hand-off explicitly.

- [ ] **Step 3: Wire the subcommand** — `cli/interface.rs`, both tables in
  `cli/mod.rs` (`:83-96` and `:123-136`), `INTERFACE_USAGE`.

- [ ] **Step 4: The four drift guards** (above). Run each one individually
  before the full gate: `cargo test -p mtc-turing-machine --test
  completions_registry`, `--test man_page`, `--test cli_docs`.

- [ ] **Step 5: Gate** — `cargo nextest run -p mtc-turing-machine`;
  compiled-stdlib byte identity (this task must not touch codegen, so it holds
  exactly).

- [ ] **Step 6: Commit** — `feat(cli): tmt interface prints a unit's
  declarations as a header`.

---

### Task 5: Declarations-only reading, and `.tmc` 0.2

**Requirements**

- **The grammar delta is one alternative.** `parse_reuse` (`parser.rs:1443-1460`)
  accepts `;` in place of `{` after a signature, yielding a routine or graph
  with no body. Established fact 6 pins that this is the WHOLE delta: a header
  carrying `export alphabet`, `namespace`, a `?` doc line, a signature and a
  `writes` clause reaches column 61 of line 4 before failing on exactly that
  brace. **[tool-verified]** — log row C1.
- **A `.tmh` is not a second grammar.** It is the `.tmc` grammar read in
  declarations-only MODE: the same lexer, the same green parse, the same
  `extract_program`. The mode is a flag on the reader, not a fork, which is
  what makes `--extern foo.tmc` (R-3a-5) free.
- Declarations-only mode additionally REJECTS what a header may not carry — a
  `machine` block, and a routine WITH a body — each with its own message. A
  graph with a body is legal and required (a graph's only form is its source).
- `TMC_LANG_VERSION` `0.1` → `0.2` **here** (R-3a-2: the first task that
  changes the grammar), and stays 0.2 through 3b.
- Green-tree work: a bodiless `Reuse` node has no `World` child. Every typed
  view and every `extract` path that assumes one must handle `None`
  (`syntax/views.rs`, `syntax/extract.rs`). **The green tree is held to
  `text() == source` over the shipped corpus and over generated programs** —
  that invariant covers `.tmh` sources too once they are parsed by this route,
  so add a header to whatever corpus that property test walks.
- `fmt` prints from the raw green tree. A bodiless signature must print
  idempotently. **`tmt fmt` on a `.tmh` FILE is deferred to phase 4** (see
  Decisions, D3) — but `fmt` over a `.tmc` that contains no bodiless signature
  is unaffected, and the printer must not regress. Run
  `cargo test -p mtc-turing-machine --test comment_positions` (the 61-position
  audit) in this task's gate.

**Fixtures**

`[shape-derived: log row C1's header, which parses up to its `;`]` — the reader
does not exist until this task. **Step 1 is to run C1's exact file through the
new reader and confirm it is accepted**, then extend.

Firing / near-miss pairs for the two new rejections:

| Fires | Near miss |
|---|---|
| a `.tmh` containing `machine { … }` | the same file with the `machine` block removed |
| a `.tmh` containing `export routine r(tape t: a) { … }` (a body) | the same routine with `;` |
| — | a `.tmh` containing `export graph g(tape t: a, state d) { … }` (a body — **legal**) |

The third row is the one that catches the easy over-correction: banning all
bodies in a header would break graphs, which is the entire reason headers carry
graph bodies at all.

**TDD steps**

- [ ] **Step 1: Run log row C1's header through the new reader** before writing
  expectations. Record the result.

- [ ] **Step 2: Tests** in `crates/turing-machine/tests/header_roundtrip.rs`:

  - `a_bodiless_signature_parses_and_carries_its_contracts` — the `writes`
    clause survives. **Mutation:** skipping the signature when no body follows.
  - `a_header_may_not_declare_a_machine` / `a_header_without_a_machine_is_fine`
    — the pair. **Mutation:** dropping the rejection.
  - `a_header_may_not_carry_a_routine_body` / `a_header_may_carry_a_graph_body`
    — the pair. **Mutation:** banning every body; the graph half goes red.
  - `a_bodiless_signature_in_a_tmc_program_is_an_error` — the mode flag is
    real, not cosmetic: a `.tmc` compiled as a program still requires a body.
    **Mutation:** making the alternative unconditional.
  - `the_language_version_is_two` — a bare assert with a comment naming what
    the version means (an acceptance contract; N bumps on ANY grammar change,
    no patch digit). **Mutation:** leaving it at 0.1.

- [ ] **Step 3: UN-IGNORE Task 4's `interface_output_reparses_as_a_header`**
  and make it green. This closes the generator/reader loop and is the task's
  headline result. It gets its own checkbox because an `#[ignore]`d test is
  invisible in a green run, and a missed hand-off would leave the loop unproven
  while everything looked fine. **Record the skipped-test count before and
  after** and confirm it dropped by one — phase 2's precedent, where Task 11
  shipped four `#[ignore]`d tests and Task 12 carried the instruction with the
  count in the ledger.

- [ ] **Step 4: Grammar + mode.** `parser.rs` for the alternative,
  `header.rs` for the mode and the rejections.

- [ ] **Step 5: Green tree and fmt.** Views, extraction, and the corpus
  property test. Run `--test comment_positions` and the `.tmc` golden test.

- [ ] **Step 6: Gate** — `cargo nextest run -p mtc-turing-machine`;
  compiled-stdlib byte identity at both opt levels (a grammar change that
  silently altered parsing would move it).

- [ ] **Step 7: Commit** — `feat(turing-machine): the .tmc grammar reads
  declarations only, and the language moves to 0.2`.

---

### Task 6: `--extern` and `--nostdlib` on `compile`

**Requirements**

- `tmt compile INPUT.tmc --extern FILE` (repeatable). `FILE` is a `.tmh` or a
  `.tmc`, read in declarations-only mode either way (Task 5), and pushed into
  `Declarations` with `Origin::Extern(path)`.
- `tmt compile --nostdlib` means "do not read the embedded `std.tmh`" — i.e.
  `Declarations::none()` as the base instead of `Declarations::stdlib()`.
  `std::…` names then become external like any other, and **an explicit
  `--extern std.tmh` still works**: a user may disable the built-in stdlib and
  supply their own library under `std`. That combination is a test, not a
  remark.
- A `--extern` file that does not parse is a compile error naming the FILE, not
  the input — the easy mistake is to report the primary input's path.
- Order matters and must be pinned: `--extern` files in command-line order,
  then the embedded stdlib last, so a user's `std` shadows the built-in one
  when both are present. **This mirrors the linker's own first-wins rule**
  (`crates/core/src/linker/resolve.rs`), and mirroring it is deliberate — a
  compiler that resolved a name differently from the linker would produce
  diagnostics about a program the linker then builds differently.
- Drift guards: `completions/registry.rs`'s `compile_spec()` gains `--extern`
  (repeatable, `.tmh`/`.tmc` extension filter) and `--nostdlib`;
  `COMPILE_USAGE` (`cli/build.rs:20-38`) gains both lines; `docs/tmt/cli.md`'s
  compile block is byte-compared by `cli_docs.rs`, so it is edited **here**.
  The man page follows `usage_text` automatically.

**Fixtures** `[shape-derived: Task 5's reader plus log row A1]`

The headline fixture is log row A1's program with its declarations supplied:

```tmc
// app.tmc — compiles with --extern mylib.tmh, fails without it
use mylib::bits;
machine {
  tape num: bits;
  entry state m { [*] -> call mylib::plusOne(num = num) then d; }
  state d { [*] -> stop; }
}
```

Without `--extern` this is `unresolved-alphabet` on `bits` (log row B3's exact
shape) **[tool-verified]** for the negative half. With it, it compiles — which
is Task 8's and Task 10's work, so **in THIS task the fixture stops at "the
declarations are in the table"**, asserted structurally, not through a
successful compile. Say that in the task: a test that asserts a compile the
next two tasks enable would be a green test that proves nothing here.

**TDD steps**

- [ ] **Step 1: Tests** in `crates/turing-machine/tests/extern_declarations.rs`:

  - `an_extern_header_reaches_the_declarations_table` — compile with
    `--extern`, assert the table holds a module with `Origin::Extern` and the
    expected exported names. **Mutation:** dropping the push; the table is
    empty.
  - `an_extern_tmc_is_read_the_same_way` — the same assertion over a `.tmc`
    whose routine bodies are present and dropped. **Mutation:** requiring a
    `.tmh` extension (which would also violate the never-dispatch-on-extension
    rule).
  - `a_broken_extern_names_its_own_file` — the error message carries the
    `--extern` path. **Mutation:** reporting the primary input's path; assert
    on the path, not just on failure.
  - `nostdlib_empties_the_table` / `without_nostdlib_the_stdlib_is_present` —
    the pair. **Mutation:** ignoring the flag.
  - `an_explicit_std_header_under_nostdlib_shadows_nothing_and_is_used` —
    `--nostdlib --extern my_std.tmh` where `my_std.tmh` declares
    `namespace std`; assert the table holds exactly that module.
    **Mutation:** re-adding the embedded stdlib when any `--extern` is given.
  - `extern_order_is_command_line_order_then_stdlib` — two `--extern` files
    both declaring `lib::sym` with different glyphs; assert the FIRST wins.
    **Mutation:** reversing the order, or pushing the stdlib first.

- [ ] **Step 2: Flags + plumbing** — `cli/build.rs::compile`, `argv_compile_options`.

- [ ] **Step 3: Drift guards** — registry, `COMPILE_USAGE`, `docs/tmt/cli.md`.
  Run `--test cli_docs` and `--test completions_registry` individually.

- [ ] **Step 4: Gate** — `cargo nextest run -p mtc-turing-machine`;
  compiled-stdlib byte identity.

- [ ] **Step 5: Commit** — `feat(cli): tmt compile reads external declarations`.

---

### Task 7: `std.tmh`, derived and pinned

**Requirements (R-3a-3 governs this task entirely)**

- `crates/turing-machine/src/stdlib/std.tmh` is committed, **generated by
  `tmt interface std.tmc`, never hand-edited**. The file gets a header comment
  saying so, in the style of a golden.
- `stdlib::header()` beside `object()` and `resolved()`:
  `include_str!("std.tmh")`, parsed once per process into a `Resolved` through
  the declarations-only reader, in a `OnceLock`.
- `Declarations::stdlib()` is re-pointed at `header()` rather than at
  `resolved()`. **This is the substantive change**, and it has a consequence to
  state: `resolved()` is the analysis of `std.tmc` (bodies and all) and
  `header()` is the analysis of its declarations. For the footprint inference
  they must agree on every `writes` contract, because that is all the inference
  reads. **Pin that**: a test asserting the two agree on the declared
  contracts of every exported routine. If they diverge, `tmt interface` is
  dropping something — which is a Task 4 defect surfacing here, exactly where
  it should.
- `std.tmc`'s own text does NOT change (R-3a-3 — the stdlib rewrite, including
  the header comment and the exported named map, is phase 4).
- The pin and the regen follow the goldens' pattern:

  ```rust
  #[test]
  fn std_tmh_is_what_tmt_interface_prints() { … }

  /// Regenerate `std.tmh` from `std.tmc`. Explicit and `#[ignore]`d, like the
  /// program goldens: the file is derived output and is never edited by hand
  /// (docs/tmt/stdlib.md (the shipped header)).
  #[test]
  #[ignore]
  fn regen() { … }
  ```

**TDD steps**

- [ ] **Step 1: Generate and READ.** Run `tmt interface` over `std.tmc`,
  read the output, and record it in the task report. Look specifically for:
  every `? ` doc line preserved, both `symbols` alphabets present with their
  glyphs in band order, every `export graph` body complete, and no `main`.

- [ ] **Step 2: Tests** in `crates/turing-machine/tests/stdlib_header.rs`:

  - `std_tmh_is_what_tmt_interface_prints` — the byte pin. **Mutation:** any
    change to the printer, which is the point: the pin is the standing proof of
    the generator.
  - `regen` — `#[ignore]`d.
  - `the_header_and_the_source_agree_on_every_declared_contract` — described
    above. **Mutation:** dropping the `writes` clause from the printer; the
    header would still parse, and only this test would notice.
  - `every_exported_stdlib_graph_is_in_the_header_with_its_body` — a count plus
    one body spot-check. **Mutation:** printing graph signatures without
    bodies, which would compile and would silently make every library graft
    impossible.

- [ ] **Step 3: Wire `header()`** and re-point `Declarations::stdlib()`.

- [ ] **Step 4: Gate** — full `cargo nextest run --workspace`.
  **Compiled-stdlib byte identity at both opt levels is the key gate here**: the
  object is built from `std.tmc` and must not move because the DECLARATIONS
  source changed. It also exercises the `OnceLock` interaction that phase 2's
  ledger records as a nextest-only failure — so this task's gate is nextest, not
  `cargo test`.

- [ ] **Step 5: Commit** — `feat(turing-machine): the standard library ships a
  generated header`.

---

### Task 8: Qualified alphabet references, alphabet imports, and the `unresolved-alphabet` split

**PULLABLE: no** — Task 10's symbolic emission is far more useful with imports,
and `Interface.imports` (brief carry 2) has been dormant since phase 2.

**Requirements**

- A qualified name is accepted **wherever an alphabet is referenced**: a
  `machine`/`routine` tape declaration (`tape num: std::binaryNumbersBare::symbols;`)
  and a signature tape parameter (`routine r(tape t: lib::sym)`). Both are
  one-line grammar changes at `parser.rs:1512` and the `tape` parse, and both
  failure messages are tool-verified (log rows B1, B2).
- `use lib::bits;` followed by `tape d: bits;` resolves when `lib`'s
  declarations are in the table. The asymmetry the spec names —
  `call`/`graft`/`bind` targets take qualified names without `use`, alphabets
  did not — closes.
- **`unresolved-alphabet` splits into two messages under one code:**
  - *no such alphabet* — nothing in scope and nothing declared, today's text.
  - *declarations not given* — the name resolves to an import or a qualified
    path whose unit is not in the declarations table. The message says so and
    names the remedy: "declared by `use`, but its declarations were not given —
    pass `--extern` or declare it locally". `Origin` (Task 1) is what lets the
    message be specific about which unit.
  The **code stays one** (`unresolved-alphabet`), so `error_code_docs.rs`'s set
  compare does not move; only `docs/tmt/cli.md`'s row text widens. State that
  explicitly — the alternative (two codes) would be a wire change to the shared
  allow namespace for no gain.
- **`Interface.imports` is filled** (brief carry 2): every alphabet the unit
  imported from another, with the glyph list it compiled against, in the
  object's interface section. The linker's `AlphabetDrift` check
  (`crates/core/src/linker/interface.rs`) becomes live. **Read the wire order
  before writing**: `imports` comes AFTER `graphs` (`docs/formats.md:254-255`;
  phase 2's Task 15 report records that hand-pinned offsets shifted by 4).
- The `; import alphabet <name>: (<glyphs>)` disassembly comment already exists
  core-side; nothing to add.

**Fixtures** `[shape-derived: log rows B1/B2/B3 plus Task 6's `--extern`]`

Four, in two pairs:

| Fires | Near miss |
|---|---|
| `tape d: bits;` after `use lib::bits;` with NO `--extern` → *declarations not given* | the same with `--extern lib.tmh` → compiles |
| `tape d: nosuch;` with no import at all → *no such alphabet* | `tape d: bits;` with a LOCAL `alphabet bits { … }` |

Plus the drift pair: a `.tmh` declaring `bits` as `{'_','0','1'}` against a
library object exporting `{'_','1','0'}` → `AlphabetDrift` at link; the same
glyph order → links clean.

**The drift pair is the test that proves the import is real** rather than
decorative, and it is the one that exercises phase 2's dormant check. Build it
even though it spans two stages.

**TDD steps**

- [ ] **Step 1: Run the four `[shape-derived]` fixtures** through
  `tmt compile --extern` as they become buildable, and record the outputs.

- [ ] **Step 2: Tests** in `crates/turing-machine/tests/cross_unit.rs`:

  - `a_qualified_alphabet_names_a_tape` and
    `a_qualified_alphabet_names_a_signature_parameter` — the two grammar
    positions. **Mutation:** accepting `::` in only one of them (the likely
    half-fix); one test goes red.
  - The four-fixture table above, each asserting the message TEXT, not merely
    the code — the split is invisible at code granularity.
    **Mutation:** collapsing the two arms back to one message.
  - `an_imported_alphabet_is_recorded_in_the_interface_section` — with its
    glyphs in band order. **Mutation:** recording the name without the glyphs,
    which leaves `AlphabetDrift` unable to fire.
  - `a_drifted_import_is_refused_at_link` / `a_matching_import_links` — the
    pair, driven through `tmt compile --extern` then `tmt link`.
    **Mutation:** filling `imports` with the LOCAL glyph list rather than the
    one the header gave; the refusal half goes green when it should be red,
    so assert the error variant, not just failure.

- [ ] **Step 3: Grammar, resolution, the split, the emission.**

- [ ] **Step 4: Gate** — `cargo nextest run -p mtc-turing-machine` and
  `-p mtc-core`; compiled-stdlib byte identity; `--test error_code_docs`
  (the row text widened, the set did not).

- [ ] **Step 5: Commit** — `feat(turing-machine): alphabets cross a unit
  boundary by name`.

---

### Task 9: Named symbol maps

**Requirements**

- `map NAME: SRC -> DST { pairs }` as a top-level declaration, `export map`
  alongside `export alphabet`, `use lib::wideToBits;` to import one. `map` is
  already reserved (Established fact 7), so this costs a top-level arm at
  `parser.rs:1270-1355` and an `export` arm at `:1324-1349`, plus the two
  recovery word sets at `:1132` and `:1210`.
- `with map NAME` at a site, alongside the inline `with map { … }` which stays.
- **Four declaration checks, checked ONCE at the declaration** (the spec's own
  list), each with a firing and a near-miss fixture:

  | Check | Message subject |
  |---|---|
  | every pair's `src` is in SRC and `dst` is in DST | the offending glyph and which side |
  | the blank is pinned (index 0 → index 0) | the pair that moves it |
  | injective on equal cardinalities | the two `src`s colliding on one `dst` |
  | closed on unequal cardinalities | the unmapped `src` |

  These reuse the inline map's existing checks wherever the inline form already
  performs them — **find out which, first**: `grep -n 'MapSymbolNotInAlphabet'
  -r crates/turing-machine/src` and read `ir.rs:786-800`. A check the inline
  form does NOT perform is a new error code and needs a `docs/tmt/cli.md` row;
  one it does perform is a reuse. **The plan does not guess which:** step 1 of
  this task is to determine it and report the answer, because it decides
  whether `error_code_docs.rs`'s set moves.
- At each SITE only two things are checked: the caller tape's alphabet is the
  map's source, and the callee parameter's alphabet is its target.
- A named map **expands to its pairs** before IR; the linker never sees a name.
  For an external callee the pairs are emitted symbolically (Task 10) — a named
  map is not a third form.
- Maps go into `.tmh` (`export map` printed by Task 4's printer — **this task
  extends that printer**, and Task 7's `std.tmh` pin does not move because
  `std.tmc` exports no map until phase 4).
- **Two lints ride along** — see Decisions (D4) for why they are here and not
  counted among "the two new lints":
  - `unused-map` (default-on), beside `unused-alphabet`. A named map nothing
    uses. Quickfix: remove the declaration, span from `spans.rs`'s
    `decl_span::<…>` family over the new node kind.
  - `dead-map-pair` **extended to named maps** — today it reads inline maps
    only (`lint/rules/dead_map_pair.rs`).

**Fixtures** `[shape-derived: log row C2's slot plus the inline `with map { … }`
grammar the parser already accepts]`

The spec's own example is the headline:

```tmc
map wideToBits: wide -> bits { '^' => '_', '$' => '_', '0' -> '0', '1' -> '1' }

bind plusOne(num = data with map wideToBits) as inc;
[*] -> call invert(num = data with map wideToBits) then t;
```

Note it mixes `=>` (one-way) and `->` and is **not** injective — three `src`s
over a 3-glyph target with `'^'` and `'$'` both collapsing to `'_'` — so it is
the closed-on-unequal case (5 → 3), not the injective one. **That makes it the
wrong fixture for the injectivity check**; build a separate equal-cardinality
one for that. Getting this wrong is the plan-defect shape the fixture rules
warn about, so it is called out here.

**TDD steps**

- [ ] **Step 1: Determine which of the four checks the inline form already
  performs**, and report. This decides whether new error codes are needed.

- [ ] **Step 2: Tests** in `crates/turing-machine/tests/named_maps.rs`:

  - `a_named_map_declares_and_is_used_by_name` — compiles; the emitted binding
    is byte-identical to the same site written inline. **Mutation:** expanding
    to a different pair order; the byte compare catches it. **This is the
    task's central claim** — a name is a spelling, not a semantics.
  - Four firing / near-miss pairs, one per declaration check.
  - `a_site_whose_caller_alphabet_is_not_the_maps_source_is_refused` /
    `the_matching_site_compiles`. **Mutation:** checking only the target side.
  - `an_exported_map_reaches_the_header` and
    `an_imported_map_is_usable_at_a_site`.
  - `unused_map_fires_on_an_unused_declaration` /
    `and_not_on_a_used_one`. **Mutation:** counting a declaration as used by
    its own declaration site.
  - `dead_map_pair_fires_on_a_named_map` — with a near miss. **Mutation:**
    leaving the rule on inline maps only.
  - Both lints: the **comment guard** must withhold the quickfix when a comment
    lands in the edit span (`run_rules`' shared guard). One test each,
    following an existing rule's pattern.

- [ ] **Step 3: Grammar, node kinds, views, extraction, checks, expansion,
  printer, lints.**

- [ ] **Step 4: Drift guards** — `docs/tmt/lint.md` gains two rows (the file is
  set-compared against the rule tables); `error_code_docs.rs` moves only if
  step 1 found a new code; `editor_grammar.rs` does NOT move (`map` was already
  a keyword).

- [ ] **Step 5: Gate** — `cargo nextest run -p mtc-turing-machine`;
  compiled-stdlib byte identity; `--test comment_positions`.

- [ ] **Step 6: Commit** — `feat(turing-machine): symbol maps can be declared
  once and used by name`.

---

### Task 10: Symbolic emission, and `external-binding-unsupported` retired

**The target is fixed, not guessed.** Established fact 8: the exact `.tma` this
task must produce assembles, links (`1 composite(s), 4 B compose table`) and
disassembles back to the same operand today. **[tool-verified]** — log rows
D1–D4. The implementer's job is to make the compiler print that text, and the
test is a byte compare against it.

**Requirements**

- `resolve_binding` (`ir.rs:709-806`) keeps today's index path for an in-unit
  callee and takes a symbolic path for an external one. The refusal at
  `:735-741` is deleted and `CompileErrorKind::ExternalBindingUnsupported` with
  it.
- **What the symbolic form carries** (P6, and the phase-1 wire rulings):
  - `param` — the callee's parameter NAME, from the declarations table.
  - `caller_tape` — a physical index, as today. Always numeric: it names the
    CALLER's own band, which the compiler knows.
  - `src` — a caller-alphabet index, as today. Same reason.
  - `dst` — the callee glyph LABEL, because the callee's index space is the
    linker's to resolve.
  - **An omitted map emits NO pairs** (P6) — index identity, and the linker's
    item-4 warning stands guard. Do not "helpfully" expand an omitted map into
    identity pairs; that would silence the warning the arc exists to raise.
  - A named map (Task 9) expands to its pairs first and is then emitted by the
    same path — no third form.
- **Entries are named or positional, never mixed** in one list (phase-1
  ruling; the assembler enforces it and the pre-pass refuses a mix). Since an
  external callee's entries are ALL named, this is satisfied by construction —
  but assert it, because a partially-resolved callee is exactly the bug shape.
- **A labelled pair carries `dst` 0 on the wire** (phase-1 ruling, carried in
  above): the wire gives that field to the label, so writing a resolved index
  back would break `from_bytes(to_bytes(x)) == x`. The IR's `IrMapDst::Label`
  must therefore serialize as a label and the object writer must put 0 in the
  index field. Verify against `crates/core/src/formats/object/write.rs` before
  relying on it.
- **Ordering.** `resolve_binding` today walks `callee.tapes` and finds each
  one's argument, so entries come out in the CALLEE's tape order. For an
  external callee the declarations table supplies that order. If the callee's
  declarations are NOT available (no `--extern`, no sibling, no library), the
  site keeps its source order and every entry is named — which is precisely
  what the linker's `reorder_named` exists for. **Both cases must be tested**,
  and the second is the one that makes "a bare `tmt compile` with no knowledge
  of any library still produces a linkable object" true.

**The emission branch** (replacing `ir.rs:735-741`):

```rust
    // An EXTERNAL callee's tape order, parameter names and glyph indices are
    // the linker's to resolve: the compiler emits a symbolic record — the
    // parameter's name where an in-unit site writes a position, the callee
    // glyph's label where it writes an index — and the link-time pre-pass
    // turns it back into the index-based binding the composition engine
    // consumes (docs/formats.md (bound calls)).
    //
    // `caller_tape` and `src` stay numeric in both forms: both name the
    // CALLER's own bands and alphabet, which this unit declares.
    if external {
        let callee_decl = decls.routine(target);
        let mut binding = Vec::with_capacity(named_args.len());
        // With declarations, entries go out in the callee's tape order and
        // every parameter is checked here; without them, source order and
        // named entries, which `reorder_named` fixes at link time.
        let order: Vec<&BindingArg> = match &callee_decl {
            Some(sig) => order_by_callee_tapes(sig, &named_args, site)?,
            None => named_args.clone(),
        };
        for arg in order {
            let BindingValue::Named { target: host_name, map, .. } = &arg.value else {
                unreachable!("named_args are Named by construction");
            };
            let (phys, host_tape) = host_tape_of(host, host_name, arg.name_span)?;
            let host_glyphs = &expanded.alphabets[&host_tape.alphabet].glyphs;
            let mut pairs = Vec::new();
            if let Some(m) = map {
                for p in &m.pairs {
                    // `src` resolves against the CALLER's alphabet, which is
                    // this unit's; `dst` travels as a label.
                    let src = glyph_index(host_glyphs, &p.src).ok_or(CompileError {
                        span: p.src.span(),
                        kind: CompileErrorKind::MapSymbolNotInAlphabet(glyph_label(&p.src)),
                    })?;
                    pairs.push(IrMapPair {
                        src: src as u32,
                        dst: IrMapDst::Label(glyph_label(&p.dst)),
                        one_way: p.arrow == MapArrow::ReadOnly,
                    });
                }
            }
            binding.push(IrTapeBinding {
                param: Some(arg.name.clone()),
                caller_tape: phys as u32,
                pairs,
            });
        }
        return Ok(binding);
    }
```

`host_tape_of` and `order_by_callee_tapes` are extracted from today's in-unit
loop so both paths share them — the in-unit branch keeps its behaviour exactly,
which is what the byte-identity gate below checks.

**`render_binding`** (`codegen.rs:658-681`) gains two spellings, from the
tool-verified target text: an entry with a `param` prints `name: phys`, and a
labelled `dst` prints as a glyph literal — `[num: 1{3->'0', 4->'1'}]`.

**Fixtures**

- **[tool-verified]** log rows D1–D4: `mylib.tma` / `app.tma`, assembled,
  linked and disassembled.
- **[tool-verified]** log row A1 for the negative that disappears.
- `[shape-derived: D2 plus Task 6's `--extern`]` the `.tmc` pair that must
  produce D2's text.

**TDD steps**

- [ ] **Step 1: Tests** in `crates/turing-machine/tests/cross_unit.rs`:

  - `an_external_bound_call_emits_the_symbolic_operand` — compile the `.tmc`
    with `--extern mylib.tmh`, `-S`, and byte-compare the `call` line against
    `call    mylib::plusOne [num: 1{3->'0', 4->'1'}]`. **Mutation:** emitting
    `dst` as an index; the compare goes red, and the link-level test below goes
    red differently (the linker would resolve a label it was given a number for).
  - `the_pair_links_and_runs` — assemble both, link, run, assert the tape.
    **Mutation:** swapping the two pairs' `dst` labels; the run's final tape
    differs. **Seed the tape** so the run discriminates — phase 2's Task 16
    review found blank-tape runs non-discriminating, and that finding applies
    here verbatim.
  - `an_external_bound_call_without_declarations_still_compiles_and_links` —
    no `--extern`; every entry named, source order, linked against the real
    library. **Mutation:** requiring declarations; compilation fails. This is
    the test behind the spec's "a bare `tmt compile` … still produces a
    linkable object".
  - `an_omitted_map_emits_no_pairs` — and the linker's `glyph-mismatch` fires
    on it when the glyphs differ. **Mutation:** expanding to identity pairs;
    the warning stops firing, which is the silent-failure shape.
  - `with_map_empty_silences_the_warning` — `with map { }` on the same site.
    **Mutation:** treating `{}` as an omitted map (`map_written` false); the
    warning returns.
  - `an_in_unit_bound_call_is_byte_identical_to_before` — the regression gate
    over a fixture from the existing corpus. **Mutation:** taking the symbolic
    path for an in-unit callee.
  - `a_missing_parameter_is_a_link_error_not_a_compile_error` — when
    declarations are absent. With declarations present it is a compile error
    naming the parameter. **Both**, as a pair: the spec is explicit that
    `missing-arg`/`unknown-arg` for an external callee become link errors.

- [ ] **Step 2: Implement**; extract the two shared helpers first so the in-unit
  path's byte identity is provable before the new branch exists.

- [ ] **Step 3: Retire the code** — delete
  `CompileErrorKind::ExternalBindingUnsupported`, its `code()` arm
  (`compiler.rs:383`), its `docs/tmt/cli.md:250` row, and the stale
  "composition engine not yet online" comments at `compiler.rs:270-279` and
  `:2809`. `error_code_docs.rs`'s set compare goes red until BOTH sides move —
  that is the guard working.

- [ ] **Step 4: Gate** — `cargo nextest run -p mtc-turing-machine`;
  compiled-stdlib byte identity (the stdlib calls nothing external, so it must
  hold exactly); `--test error_code_docs`; the everything-matrix and
  `mode_equivalence`.

- [ ] **Step 5: Commit** — `feat(turing-machine): a bound call into another
  unit emits a symbolic site`.

---

### Task 11: `state` parameters lower to `retx`

**The target is fixed.** Established fact 9 / log rows D5–D6: `.routine sub,
tapes=1, alpha=(3), exits=2, noreturn` with `retx #0`/`retx #1` and
`call sub [t: 0] exits=(hit, miss)` assembles and links under mono as one
stamp. **[tool-verified]** And log row A2 is the `.tmc` that must produce it —
`state` parameters already PARSE and already reach IR lowering, failing only at
`resolve_state`.

**Requirements**

- `ir.rs:600-616`'s `resolve_state` stops raising
  `StateParamContinuationUnsupported` and instead yields
  `IrTransition::ReturnExit { exit }`, where `exit` is the parameter's index in
  `ew.state_params`. That index IS the exit number — pin it, because the whole
  wire contract (`retx #k` leaves through exit `k` of the site's vector) rests
  on the two orders agreeing.
- A call site passing `state` arguments fills `CallThen.exits` with the resume
  states, in the callee's `state_param` order. **Same ordering obligation, other
  end** — and it is the ordering that a test must break to prove itself
  (mutation: reverse the vector; the two-exit run takes the wrong branch).
- `IrWorld.exits` is set to `state_params.len()`; codegen prints
  `exits=<K>` on `.routine` when K > 0 and the `exits=(<labels>)` operand on the
  `call`.
- **`then` stays mandatory** in 3a except against a known `noreturn` callee —
  that rule and its `unreachable-continuation` lint are Task 12. Here, a
  routine with `state` parameters still writes `then`.
- **Every optimizer pass learns the new variants.** `ReturnExit` is a
  terminator like `Return` (no successor in-world); `CallThen.exits` are
  in-world successors and MUST be walked by every reachability and
  liveness computation. The passes to audit, from
  `crates/turing-machine/src/optimizer/`: `jump-threading`, `tail-call`,
  `tail-merge`, `dce`, `dead-rows`, `dispatch-select`, `inline`, and
  default-off `outline`. **`tail-call` and `inline` are the dangerous two**:
  - `tail-call` turns `call … then return` into a `TailCall`. An
    **exit-bearing** site must never become one — the callee's `retx` would
    index the ORIGINAL caller's vector. Guard it, and test the guard.
  - `inline` splices a callee's body into the caller. Inlining a routine that
    contains `ReturnExit` is only sound if the site's exits replace those
    returns. **The simplest correct rule is to refuse to inline an
    exit-bearing callee in 3a**, and say so at the pass; the multi-exit fold is
    phase 4's `outline` work. Record it as a deliberate conservatism, not an
    oversight.
- `footprint.rs` learns both variants: `ReturnExit` contributes nothing;
  `CallThen.exits` are in-world edges.
- **`-O0` bit identity**: a program using no `state` parameter must emit the
  same bytes. The `exits=` suffix is printed only for K > 0, which is what
  makes this hold structurally.
- Retire `CompileErrorKind::StateParamContinuationUnsupported`, its `code()`
  arm (`compiler.rs:384`), its `docs/tmt/cli.md:251` row, and the stale comment
  at `compiler.rs:4380-4382`.

**Fixtures** — log row A2's `stateparam.tmc` is the headline, and its expected
assembly is log row D5's `exits.tma`. Both **[tool-verified]**, from opposite
ends.

**TDD steps**

- [ ] **Step 1: Tests** in `crates/turing-machine/tests/state_params.rs`:

  - `a_state_parameter_lowers_to_retx` — `compile -S` over A2's fixture,
    assert `retx    #0` and the `exits=(…)` operand. **Mutation:** emitting
    `ret`; the assembly compare goes red.
  - `the_exit_index_is_the_parameter_position` — a TWO-exit routine, run under
    all three mechanisms, tape seeded so the two exits are distinguishable.
    **Mutation: reverse ONE end, never the shared list.** Both ends read
    `ew.state_params` — `resolve_state` for the `exit` index and the call site
    for the exits vector's order — so reversing that list reverses both
    together and the tape does not move: **that mutation is a no-op and proves
    nothing.** The discriminating mutation is reversing *either* the index
    assigned at `resolve_state` *or* the order `CallThen.exits` is built in,
    leaving the other alone. Write the test line that way. **This is the task's
    central mutation** and the reason the fixture must be seeded.
  - `the_three_mechanisms_agree` — `mono`, `frames`, `hybrid`, comparing the
    final tape AND the termination kind. Copy the harness from
    `crates/turing-machine/tests/link_matrix.rs` (`MECHS`, `build`, `run`,
    `cell_at`), and use `.zip(&results).skip(1)` — the `zip(&results[1..])`
    form misnames the mechanism in failure messages (phase-2 Task 16 Minor 3).
  - `an_exit_bearing_site_is_never_tail_called` — force `-O1` on a
    `call … then return` shape with exits. **Mutation:** dropping the
    `tail-call` guard; the program traps or runs wrong under mono.
  - `an_exit_bearing_callee_is_not_inlined` — the stated conservatism, pinned
    so it is a decision and not a drift. **Mutation:** allowing it.
  - `a_program_without_state_parameters_is_byte_identical` at `-O0` and `-O1`,
    over an existing corpus program. **Mutation:** printing `exits=0`.

- [ ] **Step 2: Lower, emit, and audit every pass.** Audit means: open each
  pass, find its match over `IrTransition`, and decide the arm explicitly. A
  wildcard arm that silently swallows `ReturnExit` is the failure mode.

- [ ] **Step 3: Retire the code** and its docs row.

- [ ] **Step 4: Gate** — `cargo nextest run --workspace`; the everything-matrix;
  `opt_equivalence` at both levels; `mode_equivalence`'s relink byte-identity
  sweep; compiled-stdlib byte identity (the stdlib declares no `state`
  parameter, so it must hold exactly); `--test error_code_docs`.

- [ ] **Step 5: Commit** — `feat(turing-machine): a routine may hand control to
  its state parameters`.

---

### Task 12: `noreturn`, and `unreachable-continuation`

**Requirements**

- **`noreturn` is a fact of the body, exported as a clause.** Whether a routine
  can return is: any `return` transition, any `then return`, or a graft exit
  bound to `return` in the expanded world — **counted conservatively, dead
  states included, so the fact is independent of `-O`**. That last clause is
  the important one and is easy to get wrong: computing it after the optimizer
  would make an object's interface depend on its opt level.
- An author MAY write `noreturn` in a signature; the compiler checks it against
  the inferred fact and errors on a mismatch (a new error code —
  `noreturn-violated` or similar; **pick the name in this task and add its
  `docs/tmt/cli.md` row**, which moves `error_code_docs.rs`'s set).
- `tmt interface` prints `noreturn` from the inferred fact (Task 4's printer).
- `IrWorld.returns` (Task 2) carries it; codegen prints `noreturn` on
  `.routine` when false. The `.tma` half is **[tool-verified]** (log row D5).
- **One new reserved word**: `RESERVED` 27 → 28 at `lexer.rs:23`, the count pin
  at `:666`, and `editors/grammars/tmc.tmLanguage.json`'s keyword tier —
  scoped `keyword.control.tmc` like every other (a drift guard, not an editor
  feature; `crates/turing-machine/tests/editor_grammar.rs` set-compares both
  directions). **Note for the report:** the spec's #121 section says "reserved
  words 27 → 30", which omits `noreturn`; the true count after 3a+3b is 31.
- **`then` becomes optional against a KNOWN `noreturn` callee** — known meaning
  its declarations are in the table. An unknown callee keeps `then` mandatory,
  since the linker does not check it. A `then` written on a known `noreturn`
  callee is the lint `unreachable-continuation` (default-on, quickfix: drop it).
- **The header-versus-object question (brief carry 1 / phase-2's S3) is NOT
  answered here.** It is answered in Task 16, and the reason is worth stating:
  for a compiler-produced object the header and the `returns` bit agree by
  construction (both come from this same inferred fact), and headers reach the
  linker not at all (P9). The only way a lie becomes observable is a call site
  that omitted its `then` into a callee that does return — which is exactly
  Task 16's predicate.

**Fixtures** — four, two pairs:

| Fires | Near miss |
|---|---|
| `routine r(…) noreturn { … return; }` → the declared-and-wrong error | the same body without the `return` |
| `call r(…) then d;` where `r` is a known `noreturn` → `unreachable-continuation` | the same site with `then` omitted |

Plus one that is neither: `call r(…) then d;` where `r` is `noreturn` but its
declarations are NOT in the table → **no finding**, `then` mandatory. That third
fixture is the one that catches a lint keyed on the callee's name rather than on
what the unit actually knows.

**TDD steps**

- [ ] **Step 1: Tests** in `crates/turing-machine/tests/state_params.rs` (the
  clause rides the same feature) and the lint's own file:

  - `noreturn_is_inferred_from_the_body_independently_of_opt_level` — compile
    the same source at `-O0` and `-O1`, assert the interface's `returns` bit is
    equal. **Mutation:** computing the fact after the optimizer; a routine
    whose only `return` is in a dead state flips between levels. **Build the
    fixture with exactly that shape** or the mutation is invisible.
  - `a_declared_noreturn_that_returns_is_refused` / `a_truthful_one_compiles`.
  - `the_interface_carries_the_returns_bit` and `tmt interface prints noreturn`.
  - The three-fixture `then` table above. **Mutation for the third:** keying
    the lint on the callee's declared clause without checking that the
    declarations were given.
  - The comment guard on the quickfix.

- [ ] **Step 2: Implement** — inference in the expanded world (before the
  optimizer), the check, the emission, the printer, the grammar word, the lint.

- [ ] **Step 3: Drift guards** — `lexer.rs:666`, `editor_grammar.rs`,
  `error_code_docs.rs` (one new row), `docs/tmt/lint.md` (one new row).

- [ ] **Step 4: Gate** — `cargo nextest run -p mtc-turing-machine`;
  compiled-stdlib byte identity **at both opt levels** — this is the gate that
  would catch an opt-dependent inference, because the stdlib compiles at `-O1`
  and its object is pinned.

- [ ] **Step 5: Commit** — `feat(turing-machine): a routine's return fact is
  inferred, declarable and exported`.

---

### Task 13: Library grafts from headers, and the `undefined-graph` split

**Requirements**

- A graph found in the declarations table is grafted exactly as an in-unit one:
  same splice, same private copy per site, same glyph identity, same
  `expand.rs`. `expand_grafts_into` (`:1282`) resolves its target against the
  declarations table when the in-unit graph map misses.
- **Each such graft records the digest of the body it spliced** in the object's
  `grafts` list (`GraftProvenance { graph, digest }`, on the wire since phase 1).
  The digest is the CRC-32 of the graph's **canonical header text** — the
  rendering Task 4's printer produces — so whitespace and comments cannot
  perturb it. **Task 4's `the_printer_is_insensitive_to_comments_and_whitespace`
  is this task's precondition**; if it is not green, stop.
- The exporting unit records the same digest per exported graph
  (`ExportedGraph { name, digest }`) — that is the OTHER half, and it is
  produced by the same printer over the same graph, so the two agree by
  construction for a compiler-produced pair. Phase 2's `GraftDrift` check
  compares them at link time and becomes live here.
- A header-only library has no object and therefore no check — the one place a
  header is trusted (P9), stated as such in the docs and pinned by a test.
- **`undefined-graph` splits into two messages under one code** (same shape and
  the same reasoning as Task 8's split):
  - *no such graph* — today's text.
  - *declarations not given* — the target names a unit whose declarations are
    absent; the message names the remedy.
- **`graft-call-unsupported` keeps its code and gains the advice**: "…a
  call-bearing graph is not spliced — write it as a routine, with `state`
  parameters if it needs several exits". `state` parameters exist as of Task 11,
  so the advice is actionable for the first time, and the stale "it awaits
  binding composition" text (`compiler.rs:638-643`) goes. **[tool-verified]**
  log row A4 is the current text.
- **A library graph with a call inside hits the same guard**, from a header as
  from source. Pin it — the spec says so explicitly and it is the shape a
  reviewer will probe.

**Fixtures** `[shape-derived: log rows A3/A4 plus Task 6's `--extern`]`

| Fires | Near miss |
|---|---|
| `graft lib::g(...)` with no `lib` anywhere → *no such graph* | a local graph of the same name |
| `graft lib::g(...)` with `use lib::g;` but no `--extern` → *declarations not given* | the same with `--extern lib.tmh` |
| a header graph whose body contains `call` → `graft-call-unsupported` | the same graph with the call removed |
| a consumer built against a `.tmh` that drifted from `lib.tmo` → `GraftDrift` at link | the matching pair → links |
| — | a header-only library (no `.tmo` in the link) → **no check**, links |

**TDD steps**

- [ ] **Step 1: Run the five rows** as they become buildable; record.

- [ ] **Step 2: Tests** in `crates/turing-machine/tests/library_grafts.rs`:

  - `a_library_graph_grafts_from_its_header` — compile, link, run, assert the
    tape. **Mutation:** resolving the graft against the in-unit map only; the
    compile fails with *no such graph*.
  - `the_spliced_body_is_identical_to_the_in_unit_splice` — the same graph
    written locally and grafted from a header produce byte-identical code
    blobs. **Mutation:** any divergence in the header path's splice. **This is
    the task's central claim.**
  - `the_graft_digest_matches_the_exporters` — assert the two `u32`s are equal
    over a compiler-produced pair. **Mutation:** digesting the source text
    rather than the canonical rendering; a comment in either file breaks it,
    so the fixture must HAVE a comment in one of them.
  - `a_drifted_header_is_refused_at_link` / `the_matching_pair_links` —
    assert the `GraftDrift` variant, not just failure.
  - `a_header_only_library_is_not_checked` — no `.tmo` in the link, links
    clean. **Mutation:** requiring the exporter; the link fails.
  - The two `undefined-graph` arms, asserting message TEXT.
  - `a_call_bearing_header_graph_hits_the_same_guard` and the reworded advice,
    asserted on the text. **Mutation:** leaving the old "awaits binding
    composition" wording.

- [ ] **Step 3: Implement** — resolution, splice, digests both sides, the
  split, the rewording.

- [ ] **Step 4: Gate** — `cargo nextest run -p mtc-turing-machine` and
  `-p mtc-core`; compiled-stdlib byte identity; `--test error_code_docs`
  (row texts widen, the set does not move).

- [ ] **Step 5: Commit** — `feat(turing-machine): a library graph is grafted
  from its header, and the splice is verified by digest`.

---

### Task 14: `duplicate-graft-instance` — PULLABLE

**PULLABLE.** It is the arc's hygiene item (spec 5b), touches nothing else, and
the controller may defer it without changing any other task.

**Requirements**

- Default-on lint, beside `unused-graft-name` in `RULES`
  (`lint/mod.rs:145`). **Keyed exactly as the expander's dedup** —
  `target ++ 0 ++ composite.key() ++ 0 ++ cont_key(cont)`
  (`expand.rs:1310-1314`) — so there are no false positives by construction.
  Established fact 10 is the evidence: two identical sites already emit one
  subgraph, and lint says nothing. **[tool-verified]** log rows F1–F2.
- Quickfix: remove the duplicate site's declaration and redirect its `goto`s to
  the surviving instance. The redirect is the part that makes the fix
  non-trivial — a `goto` naming the removed instance must be rewritten, not
  merely orphaned. If the redirect cannot be computed for a given shape,
  **withhold the fix rather than emitting a partial one** (the `dead-rule`
  precedent: two quickfixes were withheld after probing, and the reason is
  recorded).
- The shared comment guard applies: a comment in the edit span withholds the fix.

**Fixtures** — log row F1's `dupgraft.tmc` fires it, with two corrections the
implementer must make first (called out so they are not discovered late):

1. **Both instance names must be reached by a `goto`**, or `unused-graft-name`
   and `unused-graft-instance` fire and mask the new finding (log row F2 shows
   exactly that masking).
2. The near miss is the same file with the two sites differing in their
   composite or their continuation — a different `with map`, or a different
   `done =` target. **A near miss that differs only in the `as` NAME is not a
   near miss**: the name is not in the key, which is the whole point.

**TDD steps**

- [ ] **Step 1: Build the corrected fixture pair and run `tmt lint` over both**;
  record. Confirm the firing fixture produces exactly ONE new finding and the
  near miss produces none.
- [ ] **Step 2: Tests** — firing, near miss, the quickfix, the withheld-fix
  shape, the comment guard. **Mutation for the near miss:** keying on the graph
  name alone; the near miss starts firing.
- [ ] **Step 3: Implement**, reusing the expander's key function rather than
  re-deriving it — a second copy would drift and reintroduce false positives.
- [ ] **Step 4: Drift guards** — `docs/tmt/lint.md` gains a row.
- [ ] **Step 5: Gate** — `cargo nextest run -p mtc-turing-machine`.
- [ ] **Step 6: Commit** — `feat(turing-machine): duplicate graft instances are
  a lint`.

---

### Task 15: `tmt build` derives declarations

**Requirements**

- **Sibling sources.** Each unit of a target is compiled with the declarations
  of its sibling sources, read in declarations-only mode (`Origin::Sibling`).
  The overlay already parses sibling sources for their exports
  (`lsp/overlay.rs:331-371`) — same idea, now on the compile path. Each `.tmc`
  in a target remains its own compilation unit; what changes is what it KNOWS.
- **Libraries.** For each library on the `-L` path, whatever exists of
  `<name>.tmo` (its interface section) and `<name>.tmh` (graphs, maps, doc
  lines). Either may be absent; **both absent is an error**. That widens
  `find_library` (`cli/build.rs:452-460`), which today looks for `.tmo` only
  and errors otherwise.
- **Header-only libraries.** A graphs-only library's object is empty and is
  never linked. The spec records it as verified at 27 bytes. Two things follow
  and both need a test: the declarations still reach the compiler, and the
  linker never sees an object for it (so no `GraftDrift` check — Task 13's
  fifth row).
- **No manifest key** (R-3a-5). Declarations are derived from what the manifest
  already declares: the target's source set and its libraries.
- **`stdlib: false` covers both stages.** The manifest setting reaches the
  compile step as `--nostdlib`'s meaning (do not read the embedded header) as
  well as the link step, where it already applies (`driver.rs:502-505`).
- Manifest mode still **rejects** `-o/-L/-l/--nostdlib/--entry`
  (`driver.rs:154-163`) — unchanged. `--extern` is a `compile` flag and is NOT
  accepted by `build` in either mode; `build` derives.
- `docs/tmt/project.md` gains the derivation rule and the header-only library
  (the file has no byte-compare guard, but Task 17 audits its claims).

**Fixtures** `[shape-derived: Task 6's `--extern` plus the existing manifest
fixtures in `crates/turing-machine/tests/`]` — find them with
`grep -rln 'tmt.json' crates/turing-machine/tests/` and copy the shape.

**TDD steps**

- [ ] **Step 1: Tests** (a new module in the existing manifest-mode test file,
  or a new file using `fn scratch` from `mode_equivalence.rs`):

  - `a_sibling_source_supplies_declarations` — two `.tmc` in one target, one
    calling the other with a binding; builds. **Mutation:** dropping the
    sibling pass; the build fails with the *declarations not given* message.
  - `a_library_object_supplies_its_interface` and
    `a_library_header_supplies_its_graphs` — the two halves.
  - `a_library_with_neither_is_an_error` — asserting the message names the
    library. **Mutation:** falling back to silence; the build succeeds and
    then fails confusingly at link.
  - `a_header_only_library_builds_and_is_never_linked` — assert the link
    report's object list. **Mutation:** linking the empty object; the assert
    on the list catches it.
  - `stdlib_false_reaches_the_compile_stage` — a manifest with
    `stdlib: false`; a `std::` name is then external and its alphabet
    unresolvable. **Mutation:** passing the setting to the link stage only,
    which is today's behaviour — so this test is red before the change and
    green after, which is what makes it a real test rather than a description.
  - `build_rejects_extern` — the flag belongs to `compile`.

- [ ] **Step 2: Implement** — `find_library` widened, the sibling pass in
  `build_one_target` (`driver.rs:414-560`) and in `argv_mode` (`:740`), the
  `stdlib` threading.

- [ ] **Step 3: Docs** — `docs/tmt/project.md`; `BUILD_USAGE` only if a line
  changes (it should not: no new flag). If `BUILD_USAGE` does move,
  `cli_docs.rs` byte-compares it against `docs/tmt/cli.md`.

- [ ] **Step 4: Gate** — `cargo nextest run -p mtc-turing-machine`; the example
  corpus builds (`docs/examples/run.sh` or the per-example scripts);
  compiled-stdlib byte identity.

- [ ] **Step 5: Commit** — `feat(cli): tmt build derives declarations from
  siblings and libraries`.

---

### Task 16: `tail-call-no-continuation` — PULLABLE

**PULLABLE**, and it is the only task in this plan that touches
`crates/core`. See Decisions (D5) for the justification; if the controller
would rather keep the phase free of a core diff, defer the whole task and
nothing else changes.

**This task discharges TWO carries at once** (brief carries 1 and 4), and the
reason is the plan's own finding:

> The header's `noreturn` is checked at the site where a lie becomes
> observable, not by comparing two records.

For a compiler-produced object, a header's `noreturn` and the object's `returns`
bit come from the same inferred fact (Task 12) and agree by construction; and
headers reach the linker not at all (P9). The one way a wrong `noreturn` becomes
observable is a call site that OMITTED its continuation — legal only against a
declared-`noreturn` callee (Task 12) — into a callee that does return. That is
exactly the phase-2 reviewer's `tail-call-no-continuation` recommendation, and
the predicate already exists.

**Requirements**

- `DIAGNOSTIC_CODES` (`crates/core/src/linker/mod.rs:370-382`) gains a third
  entry, `tail-call-no-continuation`, with a description in the style of the
  other two. It joins the shared allow namespace automatically through
  `known_code`'s fifth arm (`lint/mod.rs:183-196`) — **assert that**, because
  phase 2's review found the fifth arm uncovered once already
  (`validate_allow_also_accepts_link_warning_codes` iterates
  `DIAGNOSTIC_CODES`, so it covers the new code for free; confirm it does).
- Raised in `check_sites` (`engine.rs`) for any `Plain` or `Bound` site that is
  the last instruction of its function AND whose callee can return. **Reuse the
  existing predicate**, do not re-derive it:
  `continuation(caller.blob.len(), addr).is_none()` and
  `callee_can_return(syntax, callee)` (`stamp.rs:1339-1344`, `:1378-1389`).
  `callee_can_return` already implements ruling 33 — it falls back to a body
  scan when the bit claims `noreturn`, because the assembler does not enforce
  the clause. Make both `pub(super)` rather than copying them.
- It is a **warning**, not an error (ruling 29, upheld by the phase-2 final
  review): the behaviour is consistent across mechanisms, it is the same class
  as any call whose continuation falls off the blob, and the exit-bearing case
  is already a hard error (`ExitBearingTailCall`). Promotable by `-Werror`,
  suppressible by `--allow` and by `lint.allow`.
- `docs/core.md`'s link-warning table and `docs/tmt/cli.md`'s gain the row —
  **in this task**, because `crates/core/tests/error_code_docs.rs` set-compares
  the registry against the docs inventory in both directions.
- **Core neutrality holds**: the check reads `FuncRef.blob.len()`, the site
  address and the callee's `returns` bit. No TM-1 or PM-1 knowledge. Test it on
  the crate-private fake dialect, as every other core link test does.
- **PM-1**: PM objects carry no interface, so `callee_can_return` answers
  `true` for every PM callee (the `_ => true` arm), and a PM program whose last
  instruction is a `call` would warn. Check whether any PM golden has that
  shape; if one does, the plan's fallback is to gate the warning on the callee
  carrying an interface at all — which is the same "degradation when the callee
  carries no interface" the spec describes elsewhere. **Determine this in step
  1**, because it decides whether PM's reports gain an unrendered diagnostic.

**Fixtures** `[shape-copied: `crates/core/tests/link_checks.rs` and
`link_exits.rs`, which phase 2 built on the fake dialect]` — plus a TM-side
`.tma` pair, which IS tool-verifiable today: a `call` as the last instruction of
a function whose callee ends in `ret`.

| Fires | Near miss |
|---|---|
| `call sub` as the function's last instruction, `sub` ends in `ret` | the same with a `stp` after the call |
| — | the same call where `sub` is `noreturn` **and its body has no `ret`** |
| — | a `noreturn` callee whose body DOES `ret` → **fires** (ruling 33) |

The third row is the one that proves the body scan is doing the work rather
than the bit — phase 2 pinned exactly that shape as
`a_noreturn_header_does_not_excuse_a_body_that_returns`, and the controller had
to run that mutation by hand. **Run it here too.**

**TDD steps**

- [ ] **Step 1: Determine the PM-1 exposure** (above) and report it before
  implementing.
- [ ] **Step 2: Tests** in `crates/core/tests/link_checks.rs` — the three rows,
  the allow-namespace membership, the `-Werror` promotion.
  **Mutation:** trusting the `returns` bit alone (drop the body scan); the
  third row goes green when it must be red.
- [ ] **Step 3: Implement** — `DIAGNOSTIC_CODES`, `check_sites`, the two
  visibility changes.
- [ ] **Step 4: Docs + guards** — `docs/core.md`, `docs/tmt/cli.md`, both
  `error_code_docs.rs` files.
- [ ] **Step 5: Gate** — `cargo nextest run --workspace`; **PM-1 byte
  identity explicitly** (`cargo test -p mtc-post-machine --test golden_programs`
  and `--test asm_volatile`), since this is the one core diff;
  `mode_equivalence`'s relink sweep.
- [ ] **Step 6: Commit** — `feat(core): a call with no continuation into a
  callee that can return is a link warning`.

---

### Task 17: Documentation and the full gate

**R-3a-7 governs.** This is the last task before the gate, and **every claim it
writes is checked against the code by the implementer**. The list below is the
claim inventory; a claim that cannot be verified is reported, not softened.

**Pages and their claims**

**`docs/tmt/language.md`** — the 0.2 reference:
1. Headers: a `.tmh` is the `.tmc` grammar read in declarations-only mode; what
   it may and may not carry (no `machine`, no routine body, graph bodies
   required); it is generated by `tmt interface` and not hand-edited.
2. `state` parameters: the site form `call r(t = x, k = next) then done`, `then`
   as the return continuation, the `state` arguments as the exits; `goto k`
   inside the body.
3. `noreturn`: inferred from the body, declarable and checked, exported;
   `then` optional only against a known `noreturn` callee.
4. Named maps: the declaration, the four checks, the two site checks, `with map
   NAME`, the inline form staying.
5. Qualified alphabet references and alphabet imports; `unresolved-alphabet`'s
   two cases.
6. **The "same glyphs, same order" text MOVES here** from
   `docs/tmt/stdlib.md:70-76`, as the description of what a transparent call
   does and what the linker checks, with importing the alphabet and mapping
   explicitly as the two alternatives to re-declaring by position.
   `stdlib.md` keeps the index table as the worked example and gains a pointer.
7. **`with map { }` documented for the first time** as "bind by index,
   deliberately" — today's identity completion, plus the instruction to the
   linker not to warn.
8. One sentence under `graft` (identical splices are one instance; the lint) and
   one under `call` (argument lists are complete; partial binding deliberately
   absent at this stage).
9. The reserved-word list: 28, with `noreturn`.

**`docs/tmt/cli.md`** — the `interface` subcommand (added in Task 4),
`--extern` and `--nostdlib` on `compile` (Task 6), the link-warning row
(Task 16), and the error-code table with **two rows retired**
(`external-binding-unsupported`, `state-param-continuation-unsupported`) and the
new rows added by Tasks 9, 12 and 13. **`cli_docs.rs` byte-compares the usage
blocks**, so any drift here is already red before this task; what this task adds
is the prose around them.

**`docs/tmt/lint.md`** — `duplicate-graft-instance`, `unreachable-continuation`,
`unused-map`, the extended `dead-map-pair`, and the reworded
`index-identity-map`: it covers the **in-unit** case and the linker covers the
cross-unit one, and the pointer in `language.md` says so plainly (spec item 4).

**`docs/tmt/project.md`** — how `build` derives declarations; header-only
libraries; `stdlib: false` on both stages. **No `tmt.json` schema bump** — the
`project` section stays 0.2 in 3a; 3b bumps it to 0.3 for `strip-asserts`.

**`docs/formats.md`** — the fourth text-expressibility caveat (Task 3 wrote the
bullet; this task checks the surrounding count and cross-references), and the
sentences where an interface-section field gains a compiler feeder rather than
only an assembler one.

**`docs/core.md`** — where the linker's checks gain a compiler feeder:
`AlphabetDrift` is live as of Task 8; `GraftDrift` as of Task 13; the plain-site
glyph checks as of Task 3. Each currently reads as structurally dormant.

**`docs/tmt/stdlib.md`** — the `std.tmh` sentence, and the correction of the
"ships as source" paragraph at `:81-88`, which the spec calls out as describing
a precondition a user can only satisfy by pasting. **Careful:** the stdlib
REWRITE is phase 4; this is a docs correction of a claim that is now false, not
a rewrite.

**`CLAUDE.md`** — the version table (`.tmc` 0.2, TM IR 4), "fourteen
subcommands" → "fifteen" (two occurrences: the architecture bullet and the CLI
section), the text-expressibility exception count corrected to four with the
list matching `formats.md`'s, and every standing-state paragraph that says
"until phase 3" or names `external-binding-unsupported` /
`state-param-continuation-unsupported` as live limits.

**The man page** is rendered automatically from `cli::usage_text`. **But read
`crates/turing-machine/src/man.rs` before touching any usage constant**:
`RELEASE_DATE` (`:31`) is held against the CHANGELOG heading for the crate
version by a test, and the crate version does NOT move in 3a (R-3a-2), so
`RELEASE_DATE` does NOT move either. A subcommand missing from
`cli::usage_text`'s table fails the man module's own tests — Task 4 handled that.

**TDD steps**

- [ ] **Step 1: Claim audit.** Go down the inventory above, one claim at a
  time, and verify it against the code, recording the `file:line` that
  establishes it. Use the five discriminating checks the repo's docs-audit
  practice records; a generic sweep produces mostly false positives.
- [ ] **Step 2: Write the pages.**
- [ ] **Step 3: Citation sweep.** Every `docs/<page>.md (keyword)` citation
  added anywhere in this phase must resolve to a real page-plus-keyword. Grep
  the whole branch's diff for the pattern and check each. A forward citation
  that is still dangling is a defect, not a deferred nicety.
- [ ] **Step 4: Policy sweep.** No `spec §N`, no `Task N`, no issue or PR
  numbers, no hosting URLs in any published page or code comment added by this
  phase.
- [ ] **Step 5: THE FULL GATE**, in this order, one at a time, in the
  foreground:

  ```
  cargo fmt --check
  cargo clippy --workspace --all-targets -- -D warnings
  cargo build -p mtc-core --no-default-features
  cargo build --workspace --lib --target wasm32-unknown-unknown
  scripts/build-wasm-bundle.sh
  node scripts/wasm-smoke.mjs target/wasm-bundle/dist
  cargo nextest run --workspace
  cargo test -p mtc-post-machine --test golden_programs
  cargo test -p mtc-post-machine --test asm_volatile
  ```

  Plus, explicitly named because they are this phase's standing gates:
  compiled-stdlib byte identity at both opt levels, `-O0` bit identity, the
  `brk` barrier, the everything-matrix, the three-mechanism `.tma` matrix,
  `mode_equivalence`'s relink byte-identity sweep, and every drift guard listed
  in Global Constraints.

- [ ] **Step 6: Write "Behaviour changes in phase 3a"** into this plan file,
  filling in the measured numbers from Task 3's sweep rather than repeating the
  prior.

- [ ] **Step 7: Commit** — `docs(turing-machine): headers, state parameters,
  named maps and the 0.2 language reference`.

---

## Behaviour changes in phase 3a

*Written by Task 17 with Task 3's measured numbers substituted for the priors
below. What follows is the plan's prediction, in the prose a release note can
quote.*

**One. Every compiled object now describes its interface.** A `.tmo` built by
`tmt compile` or `tmt build` carries parameter names, per-tape glyph lists,
`writes` contracts and exported alphabets where it carried only arities and
cardinalities. Two consequences:

- The object grows, and `tmt dis` prints `.param` lines and `; alphabet`
  comments it did not print before. The CODE is byte-identical; Task 3 asserts
  that separately, because "the object was supposed to change" is otherwise a
  place a codegen regression can hide.
- **The glyph-level link checks phase 2 landed become live.** A call site
  binding by index into a callee whose alphabet is the same size but spells
  different glyphs is now the warning `glyph-mismatch`; a narrower callee
  alphabet is `narrow-alphabet`; a wider one was already the error `CalleeWider`.
  Nothing in the shipped corpus trips any of them — `rpn.tmc`'s `bin` is
  position-identical to `std::binaryNumbers::symbols` — but **a user program
  that re-declared a stdlib alphabet in a different order has been silently
  re-labelling symbols and now says so.** That is the hazard the arc exists to
  close, and it arrives as a warning precisely so that working programs keep
  building.

**Two. Two error codes are retired, because what they stood in for was built.**
`external-binding-unsupported` — a `call`/`bind` binding tapes into a routine
defined outside the unit — now compiles, emitting a symbolic site the linker
resolves. `state-param-continuation-unsupported` — a routine handing control to
its own `state` parameter — now lowers to `retx`. Programs that previously
failed to compile now compile; no program that compiled before fails.

**Three. Declarations reach the compiler, and three diagnostics gain a second
message.** `unresolved-alphabet` and `undefined-graph` each now distinguish "no
such name" from "declared, but its declarations were not given — pass `--extern`
or declare it locally". `graft-call-unsupported` keeps its code and drops "it
awaits binding composition" for actionable advice: write it as a routine, with
`state` parameters if it needs several exits. The codes do not change; the texts
do, so a script matching on message text will see the difference.

**Four. New surfaces.** `tmt interface INPUT [-o OUT.tmh]` is a fifteenth
subcommand. `tmt compile` gains `--extern FILE` (repeatable) and `--nostdlib`.
`tmt build` derives declarations from sibling sources and from libraries with no
manifest key, and honours `stdlib: false` at the compile stage as well as the
link stage — **that last one is a behaviour change to an existing manifest
setting**, and a project relying on `stdlib: false` to affect only linking will
see `std::` names become external at compile time too.

**Five. Three new lints, all default-on.** `duplicate-graft-instance`,
`unreachable-continuation` and `unused-map`. `dead-map-pair` extends to named
maps. A `tmt lint` that was clean can now report; `--allow` and `lint.allow`
suppress them through the namespace that already exists.

**Six. One new link warning.** `tail-call-no-continuation`: a call as the last
instruction of its function, into a callee that can return. It was previously
silent and trapped `StackUnderflow` at run time. Warning, not error — the
behaviour is consistent across all three mechanisms and is the same class as any
call whose continuation falls off the blob. Promotable by `-Werror`.

**Seven. A fourth text-expressibility exception.** The `.tmc` lexer accepts a
multi-scalar grapheme cluster as one glyph; the `.tma` glyph literal does not.
Until now nothing collided, because the compiler emitted no `.param`. It does
now, so a `.tmc` alphabet containing such a glyph compiles to an object with no
assembly form. `docs/formats.md` gains the bullet and `CLAUDE.md`'s count is
corrected. No shipped program is affected; the corpus contains no such glyph.

**What does not change.** PM-1 byte identity, core neutrality, `-O0` bit
identity, the `brk` barrier, the everything-matrix, the three-mechanism matrix,
`mode_equivalence`'s relink byte identity, and the derivation-first stdlib
goldens. The crate version stays 0.5.x; the `tmt.json` `project` schema stays
0.2; `pmt` is untouched end to end.

---

## Self-review

### 1. Spec coverage

Every clause of the spec's phase-3 line, and every carry from the brief, mapped
to a task.

| Spec clause / carry | Task | Notes |
|---|---|---|
| declarations-only reading | 5 | one grammar alternative; the mode is a flag, not a fork |
| `tmt interface` | 4 | two arms, one printer, `sniff()` not extension |
| `--extern` | 6 | repeatable; `.tmh` or `.tmc`; order pinned |
| `--nostdlib` on `compile` | 6 | with the user-library-under-`std` case tested |
| `std.tmh` with its pin | 7 | derived output; `#[ignore]`d regen; contract-agreement pin |
| symbolic emission for external callees | 10 | target is tool-verified (log rows D1–D4) |
| `state`-parameter routines with `retx` | 11 | target is tool-verified (log rows D5–D6) |
| library grafts from headers | 13 | with digests both sides |
| qualified alphabet references | 8 | both positions, tool-verified failures |
| alphabet imports | 8 | fills `Interface.imports`; wakes `AlphabetDrift` |
| named maps | 9 | four declaration checks, two site checks |
| `undefined-graph` split | 13 | one code, two messages |
| `unresolved-alphabet` split | 8 | one code, two messages |
| `graft-call-unsupported` advice | 13 | actionable only once Task 11 exists |
| the two new lints | 12, 14 | `unreachable-continuation`, `duplicate-graft-instance` |
| IR v4 | 2 | one encoding task; consumers follow |
| two error codes retired | 10, 11 | each retired by the task that builds its replacement |
| language reference | 17 | with the claim inventory |
| **carry 1** — header `noreturn` vs the object's `returns` bit (S3) | 16 | checked at the site where a lie becomes observable, not by comparing records |
| **carry 2** — `Interface.imports` dormant | 8 | wire order: `imports` after `graphs` |
| **carry 3** — the text-expressibility checkpoint | 3 | probed; a real fourth exception, declared rather than spelled (D1) |
| **carry 4** — ruling 29 / `tail-call-no-continuation` | 16 | PULLABLE; small, and the predicate already exists |
| **carry 5** — `tmt build -v` output growth | — | inherited from phase 2, not added to; 3a's own output changes are items 1 and 4 of Behaviour changes |
| **carry 6** — what changes when the compiler emits interfaces | 3 | step 1 IS the measurement; Established fact 3 is the prior |

**Deliberately outside this plan, stated rather than hidden:**

- **Everything in the spec's "### Folded in from #121" section** — 3b, per
  R-3a-1. Where 3a leaves a seam it says so: the interface section's
  `enters`/`leaves`/`opaque` fields already exist on the wire (Established fact
  13) and 3a writes their empty values; `IrTape` deliberately does NOT gain
  them (Task 2), and 3b's own `TM_IR_VERSION` decision is left open.
- **`tmt lint`/`fmt` on a `.tmh`** — proposed for deferral to phase 4 (D3).
- **Everything in phase 4** — `outline` multi-exit, the LSP overlay, the stdlib
  rewrite, `docs/lsp.md`, CHANGELOG, the 0.6 release.
- **`-Werror` and `--allow` on `link`** — landed in phase 2; 3a only adds a code
  to the namespace.

### 2. Placeholder scan

Searched for "TBD", "TODO", "implement later", "fill in details", "add
appropriate error handling", "write tests for the above", "similar to Task N".
None present. **Seven places deliberately require the implementer to MEASURE or
DETERMINE rather than transcribe**, and each says exactly what to do with the
answer:

1. Task 3 step 1 — the corpus sweep, with a stop gate.
2. Task 4 step 1 — generate `tmt interface`'s output and read it before writing
   an expectation.
3. Task 7 step 1 — the same, over `std.tmc`.
4. Task 9 step 1 — which of the four named-map checks the inline form already
   performs; it decides whether `error_code_docs.rs`'s set moves.
5. Task 12 — the new error code's NAME is picked in the task.
6. Task 16 step 1 — PM-1's exposure to the new warning, with a stated fallback.
7. Task 2 step 2 — `serde`'s `untagged` behaviour for `IrMapDst`, with a stated
   fallback.

**Three places name a symbol or a rule the implementer must confirm against the
code rather than trust**, each flagged inline: the labelled-pair `dst`-0
invariant in `crates/core/src/formats/object/write.rs` (Task 10), the
`imports`-after-`graphs` wire order (Task 8), and `find_library`'s error text
(Task 15).

**Two mutations were checked for being no-ops and one was corrected.** A
mutation that both ends of a relation read alike does not discriminate: Task
11's exit-index test originally proposed "reverse `state_params`", which
reverses the lowering end AND the call-site end together and leaves the tape
unmoved. It now says to reverse exactly one end and names both. The same
discipline caught the spec's own non-injective named-map example (Task 9).
**Apply it to every new mutation an implementer writes**: before trusting a
mutation, ask what else reads the thing being mutated.

### 3. Type consistency

- `Declarations` / `Origin` — introduced Task 1, referenced by those exact
  names in Tasks 6, 8, 13, 15. `Origin::{Stdlib, Extern, Sibling, Library}`,
  four variants, never renamed.
- `Declarations::none()` / `::stdlib()` / `::push` / `::modules()` /
  `::origin_of` — five methods, Task 1, same spellings afterwards.
  `modules()` returns `Vec<&Resolved>`, the exact shape
  `ExternalContracts::modules()` returns today.
- `IrMapDst::{Index, Label}` — Task 2, consumed in Task 10 only.
- `IrTape.glyphs` / `.writes`, `IrWorld.exits` / `.returns`,
  `IrTransition::ReturnExit { exit }`, `CallThen.exits`, `IrTapeBinding.param`
  — Task 2 declares all seven; Tasks 3, 10, 11 and 12 fill them. No field is
  renamed after declaration.
- `header.rs` holds BOTH the declarations-only reader (Task 5) and the
  canonical printer (Task 4). Task 4 creates the file; Task 5 extends it. One
  module, two directions, which is what keeps `tmt interface`'s output and the
  reader's input provably the same language (Task 5 un-ignores Task 4's
  `interface_output_reparses_as_a_header`).
- `host_tape_of` / `order_by_callee_tapes` — Task 10, extracted from today's
  in-unit loop, used by both branches of `resolve_binding`. One definition each.
- `callee_can_return` / `continuation` / `check_splice_site` — phase-2 symbols,
  made `pub(super)` by Task 16, **never copied**.
- `tail-call-no-continuation` — one string, in `DIAGNOSTIC_CODES`, in
  `check_sites`, in both docs tables.
- `TMC_LANG_VERSION` moves in Task 5 and nowhere else; `TM_IR_VERSION` in
  Task 2 and nowhere else; `RESERVED`'s length in Task 12 and nowhere else;
  `RELEASE_DATE` **does not move at all**.

---

## Decisions for the controller

Every point where this plan had to choose. None is settled by the spec.

- **(D1) The text-expressibility checkpoint is a REAL fourth exception, and the
  plan declares it rather than spelling it.** Probed and confirmed (log rows
  E1–E4): the `.tmc` lexer decodes a multi-scalar ZWJ cluster as one glyph and
  pins that behaviour; core's asm lexer refuses multi-character glyph content
  and pins THAT; a `.param` carrying such a label is `error: not assembly text`.
  Today nothing collides because the compiler emits no `.param`; Task 3 wakes
  it.
  - **The plan's pick: declare the exception.** Cost: one `docs/formats.md`
    bullet (its caveat list already contains the fact from the format's side),
    one `CLAUDE.md` count correction (which is due anyway — `CLAUDE.md:213` says
    three and names a different three from `formats.md`'s), and one explicit
    test asserting the non-round-trip with its reason.
  - **The alternative: make it spellable** by widening `.tma`'s glyph literal to
    accept any quote-free content. Cost: it drops a documented rule
    (`formats.md`: "a multi-character one does not lex at all"), flips a pinned
    core test (`glyph_rejects_multi_char_content`), and puts a **second**
    `crates/core` diff in a compiler phase — for a shape the corpus does not
    contain. **Recommend against for 3a**; it is a notation change and belongs
    in phase 4 with its own review if wanted.
- **(D2) IR v4 carries three things the spec does not name.** The spec lists
  `ReturnExit`, `CallThen.exits`, `IrTapeBinding.param` and the index-or-label
  `dst`. But `.param` needs glyph labels and `writes` sets, and `.routine` needs
  the exit count and the `returns` bit — and `codegen::emit_program`'s only
  input is `IrProgram` (Established fact 4). So `IrTape.glyphs`, `IrTape.writes`,
  `IrWorld.exits` and `IrWorld.returns` are additions the spec implies rather
  than states. **The alternative — giving codegen a second input — is worse**:
  it would make the IR no longer a complete description of what is emitted,
  which is the property that makes `tmt ir` a documented artifact.
- **(D3) `tmt lint` and `tmt fmt` on a `.tmh` are proposed for phase 4** (the
  controller asked for a proposal, R-3a-4). `fmt` is nearly free off the green
  tree, but `lint` needs the per-rule "applies to headers" flag **and its drift
  guard** — a whole surface — and R-3a-4 already puts LSP routing of `.tmh` in
  phase 4. Keeping the three together is the coherent split; splitting `fmt`
  out would ship a `.tmh` surface that formats but cannot be linted or opened.
- **(D4) `unused-map` and the `dead-map-pair` extension ride with named maps
  (Task 9), which widens the spec's "the two new lints" line.** The Disposition
  names "the two lints" alongside named maps, and those two are
  `duplicate-graft-instance` and `unreachable-continuation` — but a named map
  nothing uses is a 3a-only surface, created by 3a, and shipping the feature
  without its hygiene rule would leave a gap only phase 4 could close.
  **Deliberate widening, disclosed.** Defer both if the controller prefers the
  line read literally; Task 9 stays otherwise intact.
- **(D5) Task 16 is the plan's ONE `crates/core` diff, and it is PULLABLE.**
  Core neutrality says a core diff in a compiler phase is a design smell. The
  justification: the check is arch-agnostic (it reads a blob length, a site
  address and a `returns` bit), it reuses a predicate phase 2 already wrote and
  pinned, it discharges two carries at once, and it is tested on the
  crate-private fake dialect like every other core link test. **If the
  controller would rather keep 3a free of a core diff, defer the whole task**;
  carry 1 is then still discharged in substance (Task 12 makes the compiler's
  check the authority) and carry 4 moves to phase 4 with the rest of the
  `.tma` lint candidates.
- **(S3 restated, because it is the plan's own finding)** The brief asks how
  the header check and phase 2's `callee_can_return` body scan relate. They are
  the same check, seen from two ends. A compiler-produced header and its
  object's `returns` bit come from one inferred fact (Task 12), so they cannot
  disagree; headers reach the linker not at all (P9); and the one observable
  consequence of a lie is a site with no continuation into a callee that
  returns — the predicate `check_splice_site` already computes. **There is
  nothing left to compare, and adding a record-vs-record check would be
  ceremony.** Stated so a reviewer sees it was decided, not skipped.
- **(Two codes, not four)** Both the `unresolved-alphabet` and the
  `undefined-graph` splits keep ONE code and widen to two message texts. The
  alternative — a code per case — would move `error_code_docs.rs`'s set and add
  two names to the shared allow namespace for no gain, since neither case is
  separately suppressible (both are errors). **If the controller wants them
  separately greppable, say so before Task 8.**
- **(Reserved-word arithmetic)** The spec's #121 section says "reserved words
  27 → 30 (`enters`, `leaves`, `set`)", which omits `noreturn` from the main
  section. 3a takes 27 → 28; 3b takes 28 → 31. **A spec arithmetic slip, not a
  plan deviation**, recorded so 3b's plan does not inherit it.
- **(Ordering) `--extern` files first, embedded stdlib last.** So a user's
  `std` shadows the built-in one. This mirrors the linker's first-wins rule
  deliberately: a compiler resolving a name differently from the linker would
  diagnose a program the linker then builds differently. **The alternative —
  the built-in stdlib always first — makes `--nostdlib --extern my_std.tmh` the
  only way to override, which is a worse ergonomic for no safety gain.**
- **(Conservatism) An exit-bearing callee is not inlined in 3a** (Task 11).
  Inlining one is sound only if the site's exits replace the callee's
  `ReturnExit`s — that is the multi-exit fold, and it is phase 4's `outline`
  work. Refusing is correct and costs an optimization on a construct that did
  not exist yesterday. **Pinned by a test so it is a decision, not a drift.**
- **(Conservatism) Task 3 does not emit `exits=` or `noreturn`.** Both are
  suffixes for non-default values, and until Tasks 11 and 12 every world has
  zero exits and returns. Emitting them anyway would break the writer's
  "lowest version that carries the content" rule and move every object for
  nothing.
- **(Measurement) Task 3's sweep has a STOP gate, not a fix instruction.** If a
  shipped example trips `glyph-mismatch` or `narrow-alphabet`, the implementer
  reports rather than edits the example: a shipped program that was silently
  re-labelling symbols is a finding about the corpus. Established fact 3 says
  none should. Phase 2's ruling 27 is the precedent for how such a finding is
  handled (the controller decided; the implementer did not).
- **(Task 1 cost) The stdlib's `Resolved` is CLONED per compile.** It is
  `&'static` today and free. The clone is what removes the `'static` constraint
  that blocks headers, and one type for both cases is worth it. **If a later
  task reports a compile-time regression, the alternative is a `Cow`-shaped
  `Declarations` — but do not build that speculatively**; the phase-2 ledger's
  measurement says `expand`, not resolution, dominates.
