# Build history

The phase-by-phase record of how this repo was built — what each round
shipped, in what order, and (the part no other surface carries) what was
tried, probed, and rejected along the way.

Lifted verbatim out of `CLAUDE.md`'s preamble on 2026-08-20, where it had
grown to 18.8 KB of a 39 KB file that loads into every session. It is kept
because `CHANGELOG.md` records *what shipped* and the specs record *what was
designed*, but neither records the sequencing or the discarded branches.

**This file is not loaded automatically. Read it when you need to know why
something is the way it is and the durable docs don't say.** Standing
constraints — the rules still in force — were promoted into `CLAUDE.md`'s
topical sections during the same pass and are NOT repeated here; if the two
ever disagree, `CLAUDE.md` is current and this file is history.

Internal artifact: forge references are unrestricted here.

Companion surfaces: `CHANGELOG.md` (four version blocks, ref-free prose),
`docs/superpowers/specs/` (design rationale per round),
`docs/superpowers/plans/` (execution plans), `docs/history.md` (the 2002–2012
Delphi lineage — a different thing entirely, and a durable page).

---

## The record, as of v0.4.0 (2026-08-17)

**Current state: v0.4.0 released 2026-08-17** (crates 0.4.0, `.pmc` 0.4, PM-1 `.pma` 0.3, `.tmc` 0.1, TM-1 `.tma` 0.3, PM IR 4 / TM IR 3, containers MO 3 / MX 2 / MT 2, both project-manifest schemas 0.2, all four editor plugins at 0.2.0 with `MIN_TESTED_*` floors at 0.4.0 — the debugging release: `pmt dap`/`tmt dap` + both editor pairs as clients, the optimizer motion/value round, map-sidecar source provenance). **The TM-1/tmt arc (#8) is complete** — the phase-by-phase trace follows (spec approved at `docs/superpowers/specs/2026-07-16-tm1-and-tmt-design.md`; phases 1, 2, 3a, 3b, 4a, 4b, 5a, 5b merged to master; ~1,780 tests): core groundwork (MR unification, TR bank, table engine, multi-device driver), MX v2 / MO v3 / MT v2 containers, PM companions (`wrl`/`wrr`, `fuse_tape_ops`, `pmt tape new/set`, `.pma` 0.3), the caps-gated assembler framework (sections / match+dispatch tables / `.rept` macros / vector operands / `.routine` signatures), the TM-1 crate itself — `docs/examples/brainfuck-utm.tma` (a hand-written universal Turing machine interpreting brainfuck) assembles, links, and runs, proven by derivation-first goldens — the frames execution profile (`.tma` 0.2): FR register + frame cache in the core VM, `call.m`/`retx`/`trap`, `.frame`/`.map`/`.exits` descriptor authoring — and the link-time composition engine (phase 5 complete, three-mode equivalence green): declarative binding calls lower per `tmt link --call-mech = mono | frames | hybrid`; the compose model is runtime `FR' = compose[FR][site]` through a composite directory in the MX v2 frames region (`call.m` operands are call-site indices; FR is a composite index; raw hand-authored sites are constant compose columns); mono stamps specialized copies (rmap-preimage row rewriting, synthesized trap rows, one-way row expansion, digest-named dedup); the composition algebra (`linker/compose.rs`) is law-property-tested against a brute-force oracle; `LinkOptions.entry` + LinkReport counters + map-sidecar binding records with canonical labels + the dis frames legend. Phase 6 shipped the `.tmc` language whole (`TMC_LANG_VERSION` 0.1, `TM_IR_VERSION` 2, `.tma` dialect 0.3 with `wrmv` live): lexer → lossless CST → resolution/flatten → graft splicing + range expansion (compiler-side stamping, oracle-property-tested) → per-world state-graph IR → optimizer → codegen → `tmt compile`/`ir`; the spec's six Appendix A examples compile, link, and run as derivation-first goldens. The optimizer (6b) carries the ported motion passes (`inline` as a sound superset of the engine's collapse, `jump_threading`, `tail_call` before `tail_merge`, `dce`) plus the TM-native `dead_rows` (same-band cover) and `dispatch_select` (two-rows-catch-all-last → `jm` lowering, machine-world-only), and the default-off single-junction `outline` behind `--foutline`; `-O0` bit-identity is a locked floor, the brk barrier holds per-pass, and the everything-matrix proves `-O0`/`-O1` × mono/frames/hybrid equivalence incl. trap kinds. The stdlib twins `std::binaryNumbers` (delimited, ten routines) and `std::binaryNumbersBare` (bare, four) ship embedded (`include_str!` + `OnceLock` at `-O1`-stripped, `tmt link --nostdlib` opts out), graph+facade anatomy, with the delimited `invertNumber` composed over the bare one through one-way marker collapses. PM-1 byte-identity held through every phase. Phase 7 shipped the TM tooling whole: `.tmc` and `.tma` lint layers merging turing-side over core's closed `asm::lint` (12 `.tmc` rules incl. the deferred unused-graph/binding/graft-instance family, opt-in `state-may-trap` behind `--warn`; 3 `.tma` additions; core's `unused-label` force-suppressed on the `.tma` path because `AsmLintContext` cannot see labels reached through lowered `.targets`/`.exits` — 400 false findings on the flagship without it, durable fix is core-side), one shared allow namespace across all four surfaces, `tmt.json` (nearest-ancestor, `lint.allow`, union — never a cascade), `tmt lint`/`tmt fmt`, the CST-driven `.tmc` formatter (canonical, idempotent, whitespace-only — proven by a byte-identical compiled stdlib — reproducing three hand-written fixtures exactly; its one exception, comments inside a binding/signature/alphabet body relocating, is documented and fixture-pinned), `tmt completions` (registry + bidirectional drift guards), staged analysis (`analyze_staged`, partial results at every break point), both `LanguageService`s under one `tmt lsp` (context-classified `.tmc` completions resolving the contextual tape's alphabet per cell; `.tma` at pma parity plus table-label navigation and `.frame`/`.map`/`.exits` diagnostics; CLI≡editor parity is structural — one `lint_tma` call feeds both), and the TM plugin pair (`editors/vscode-tm` + `editors/jetbrains-tm` at 0.1.0, `MIN_TESTED_TMT`, grammars in a shared `editors/grammars/` with generated bidirectional drift guards; the PM pair moved to `-pm` suffixes with plugin identity byte-identical). ~2,076 tests. Live editor verification is the maintainer's post-merge step — the sideload checklists ship unticked. Phase 8's docs half shipped: `docs/` split into per-toolchain domains — `docs/pmt/` and `docs/tmt/` each carrying language/isa/cli/stdlib/lint/fmt, a new shared `docs/core.md` for the arch-agnostic VM/assembler/linker/asm-lint (the crate boundary made durable — `crates/core` cites it and zero architecture-domain pages), and the shared root pages `formats.md`/`history.md`/`lsp.md` reworked to cover both toolchains; six new `docs/tmt/` pages, a `cli_docs` drift guard (the `tmt --help` verbatim quote — the pmt page has no such guard, tracked), the README front door presenting both machines. Every page was verified against the tools, which disproved something on each and surfaced a run of bugs now filed. The range-expression round (#31, spec `docs/superpowers/specs/2026-07-21-tmc-range-expressions-and-rept-emission-design.md`, merged 2026-07-21) settled the `.tmc` grammar under 0.1: write-fold substitutions grew to the assembler's exact expression grammar (`+ - * %` + parens, i64, compile-time fold per expanded row; passthrough is a single bare name BY TREE SHAPE — `{(c)}` stays passthrough; errors `zero-modulus` / `negative-remainder` with the `{(v+N-1)%N}` idiom hint / `fold-overflow`; negative remainder errors at BOTH ends, mirroring core's `subst.rs`); the optional transition (omitted = stay in the current state; needs ≥1 of write/move/debugger; `call … then` stays mandatory; graph rules self-loop to their spliced instance; `Transition::Stay` never reaches IR — type-level); the `.rept` re-detection emitter (`rept_emit.rs`: post-codegen text compression, runs ≥4, constant/affine/modular inference, assemble-both byte-compare self-check with stamped fallback — the pass can never change what assembles; flagship `-S` 1665→160 lines / 7 `.rept` families; `--stamped-asm` opts out; `-g` always stamped because the debug line map is stamped-line-indexed); the never-fires family (`empty-expansion` + `unreachable-rule` compile warnings — the latter deliberately NARROW: only a second all-wildcard rule, since band dispatch keeps exact/partial rules after a catch-all reachable; zero-row states are valid and trap on entry — `NoTransition` parity ≤126-wide, bare `trap` fallback at the 127-symbol ceiling where no unmatchable row can exist); and the lint wave (`.tmc` default tier `unused-alphabet`/`unused-tape`/`unused-graft-name`/`unused-exit` — the first two/last two export-independent by design; warn tier `index-identity-map` auditing omitted-map call/bind index identity, which language.md now documents as intended semantics with the graft-vs-call layer rationale; `.tma` `duplicate-map-source`, clause-generic over rmap+wmap; quickfixes on `unused-alphabet`/`unused-graft-name`/`duplicate-map-source`; `LintContext` widened additively with `program`+`tokens`). The tmc TextMate grammar's substitution operators fixed (`[-+*/]`→`[-+*%]` — the `/` was a phantom). ~2,154 tests; PM-1 byte-identity and `crates/core` zero-diff held through the round; closes #31/#33/#35/#43(by-design)/#44/#45/#46/#47/#48, #38 scoped-shipped (new rules carry fixes where safe; retrofit open). The stdlib text pass (merged 2026-07-21) made `std.tmc`'s own doc lines true (#49's three wrong claims fixed probe-first; the twelve unused `as` names dropped, lint 12→0; all 28 remaining `?` entities audited clean; compiled stdlib object byte-identical before/after at both opt levels — provably text-only; sugar adoption deliberately declined, explicit self-goto is the house style). The pre-release-polish round (7 tasks, merged 2026-07-21) then cleared the first-impression backlog: all four TextMate grammars' keyword tier re-scoped to `keyword.control.<lang>` (IntelliJ's built-in TextMate engine maps only a small scope-prefix table — the old `storage.type`/`keyword.operator.*` taxonomy rendered plain; VS Code unaffected); oversize tape alphabets (>255, the u8 count ceiling) are a typed `AlphabetTooWide` error instead of a panic; BOTH mono stamping refusals (`MonoRawFrame` AND `MonoHoleyMatchBranch`) advise `--call-mech=frames` only (hybrid's advice was probed circular — its classifier inspects top-level sites only and delegates wholesale to mono via the `!any_frames` fast path; both worlds pinned by link tests); `pmt dis`/`tmt dis` refuse foreign architectures like `run` does (executables and objects; `docs/formats.md`'s unchecked-dis-as-intentional paragraph rewritten); LSP extension routing is case-insensitive (core `bind_service`, compare-time ASCII fold — `.TMA`/`.PMA` route correctly in both servers); `jvmToolchain(17)` pinned in both JetBrains plugins and build-proven (READMEs state only verified facts; next pm-plugin build's bytecode differs from the shipped 0.1.2 — bump its version if fresh PM artifacts ever attach); and #34 landed — core's `unused-label` sees labels reached through table sections (`Dispatch.targets` + `Frame.exits`, reference-shaped feed proven live by positive controls), the `.tma` force-suppression is deleted (flagship 400 false findings → 0), `.rept`-expanded findings span the enclosing `.rept` header instead of `1:1`, caps-off dialects proven inert (PM untouched). The manifest+build design spec was amended for shipped TM-1 (`tmt build` as a first-class twin; parallel `project` schema in `tmt.json`; call-mech RULED: target-level key + project default, flag overrides — accepted in manifest mode). ~2,172 tests. The hygiene sweep (nine tasks, merged 2026-07-25) then cleared the internal-quality backlog: #22 (resolve-only assemble entry — lint and the `.pma`/`.tma` services lower once, ~11ms→~1ms on a 10k-line file), #52 (error codes as `code_registry!` const registries + per-namespace docs inventory tables + set-compare drift guards + the missing `pmt --help` quote guard), #40 (bidirectional directive drift guard over a core-owned `recognized_directives(caps)` inventory), #39 (`.tma` service spans unified half-open — the original inclusive-vs-half-open-bug premise was wrong: `.tma` was already behaviorally half-open and merely expressed containment two ways, while `.pma`'s end-inclusive completion is deliberate and kept), #20 (pmc LSP walkers/scans/extractors collapsed into a shared `walk.rs`), #21 (core LSP hardening — transport 64 MiB Content-Length cap rejected pre-alloc, post-shutdown `InvalidRequest` guard, semantic-token packer skip-on-violation fallback, position-mapping + docstore pins — plus PM `render_fatal`/`lower_and_merge` helpers and a bounded LSP config cache mirrored TM-side), and #38 (quickfix retrofit: five safe deletion fixes across the pre-existing `.tmc` rules, ten `None` with documented reasons — `redundant-identity-pairs` probed not-byte-identical so declined, `dead-rule` withheld for silent-miscompile risk). Also folded in: two parallel-run test-isolation race fixes (proven collision-free by PID + per-call atomic counter), the mechanism-neutral rewording of the `MonoRawFrame` stamping refusal, and the 127-ceiling probe (verdict: already fixed by `zero_row`'s `card > 0x7E` bare-trap fallback). ~2,226 tests; PM-1 byte-identity, `crates/core` neutrality, and version-space constants all held; zero fix waves across the round. **The project-manifest round then merged** (plan 1 of 3, merged 2026-07-25, master `ac8be9e`, ~2,329 tests): both project files grew a `project` section (schema **0.2**; the lint-only shape that predates it is retroactively 0.1) — shared and per-target sources, libraries, profiles, named targets with their own entry/output/run settings — behind a per-crate `project.rs` that is the ONE loader validating the whole file for every consumer, with per-section discovery (the `lint` and `project` sections walk independently, so a lint-only file between a source and its project root is transparent to the project walk). Both `pmt build` and `tmt build` ship as `cli/driver.rs`: argv mode (an in-memory cc driver, objects never touching disk without `--keep-objects`) and manifest mode (targets, one profile per invocation with individual flags overriding it, `--list-targets`, `--run` through a settings-split `run`), dispatching on whether the positionals look like sources or target names — mixing the two is an error. Manifest mode rejects the flags the manifest already declares (`-o`/`-L`/`-l`/`--nostdlib`, plus TM's `--entry`); `--call-mech` is the one link-side flag it ACCEPTS, as a per-invocation override of a target's committed lowering. TM divergences that are contract, not accident: `call-mech` as a project default plus a per-target key; a `tape`-only run block, because `tmt run` drives a band from a `.tmt` snapshot and has no empty-tape default, so a target without one cannot be `--run`; and a declared source set that keeps `.tma` (both TM languages have lint AND fmt) while dropping only the object extension. A bare `lint`/`fmt` — no positionals — now runs over that declared source set, never a directory scan (#28); `--no-config` is rejected on a bare `lint` because the manifest IS the input. No core change was needed. `docs/pmt/project.md` and `docs/tmt/project.md` are the new references; a full docs audit ran over the round (every quoted transcript re-run, parser↔docs flags set-compared both ways, every quoted diagnostic traced to source, lint/opcode/stdlib rosters and version constants set-compared, LSP capabilities probed through a real initialize handshake). Closed #16/#11/#28; the LSP half of #16 is now **#56**, the editor half **#29**. **Plan 2 (#56) then merged** — the LSP cross-file overlay, shipped on BOTH toolchains: for a document belonging to a project target, `pmt lsp`/`tmt lsp` now resolve names against the target's declared siblings and libraries (exported symbols only, first-source-wins, then declared libraries, then the embedded stdlib last — the linker's own order), unioned across every target a document is a member of. PM-1 gets the full surface (completion, go-to-definition, hover, semantic tokens, and the `undeclared-external` diagnostic refinement); TM-1 gets the same minus semantic tokens (the `.tmc` token layer stays purely lexical, no resolution tier added) and a narrower overlay surface by language rule — only a transparent argless call/bind and `use` paths can name something outside the current compilation unit, never a graft target, a binding name/value, a `with map` pair, or a vector-cell glyph. #37 (the `.tmc` stdlib bridge) closed as the overlay's prerequisite leg: `std::` routines now hover with real documentation, navigate into a materialized per-version `std.tmc` cache copy, and complete in `use` paths and call/bind targets, closing what had been ~108 lines of doc/attention prose invisible to the editor. Both crates carry a faithfulness fixture comparing the overlay's resolution against the real linker's, by provenance, not just by name; `docs/lsp.md` gained a full cross-file reference section documenting membership, resolution order, the per-toolchain surface, the faithfulness contract, and — honestly, not glossed — its known caveats: lexical-only path identity (no symlink resolution), cross-document staleness (a server watches only its project file, never a sibling source — a suppressed warning can outlive the export that justified it until the open document is itself touched), multi-target membership (union for completion, single-target for `build`'s own diagnostics — the two can disagree), and UTF-16 span conversion into an unopened non-ASCII file. A fmt-page parity fold-in rode the same round: `docs/pmt/fmt.md` gained the `.pma` half `docs/tmt/fmt.md` already had (extension-dispatch intro, a `.pma` formatting section with a real compiled example, the `line-too-long` claim verified PM-side). Follow-ups filed rather than built: assembly-side overlay features (`.pma`/`.tma` cross-file navigation and completion — the assembly services stay export-sources-only), a TM semantic-token resolution tier, threading `WorldKind` through TM's export record so a sibling's `main` is excluded by kind rather than by literal name match, and two of the caveats above as candidate issues (cross-document staleness, multi-target diagnostic divergence). Rename/find-references stay parked (the spec's v2 ledger). **Release path from here:** plan 3 the editor round (**#58**: per-target `build <target>` / `build --run <target>` tasks in both VS Code extensions from `--list-targets`, JetBrains recipe-only, all four `MIN_TESTED_*` floors bumped; #54's JSON Schema rides it; the `$tmt` matcher the spec lists is ALREADY shipped. Follow-ups past it: #29 JetBrains plugin-side integration, #51 prong 2 after plan 2) → #57 fmt comment re-attachment (a comment inside a comma-separated list — TM binding/signature/alphabet bodies, PM `use` paths — re-attaches to the next item; the fix is per-entry trivia on those CST list nodes, both crates, and does NOT require #14's C2 migration) as its own round, gated on whitespace-only + idempotence + a byte-identical compiled stdlib, sequenced LAST → release prep (#24 bundle both vsix) → the cut itself (CHANGELOG version block declaring `.tmc` 0.1 / `.tma` dialect 0.3 / TM IR 2 / container versions / BOTH project-manifest schemas 0.1 → 0.2, crate + TM-plugin bumps, GH release with the TM plugin artifacts, the maintainer's live sideload verification never yet ticked; closes #8). Roadmap after the cut: #5 DAP, #6 wasm, #7 async bus — parked as design inputs for the #7 optimizer-dataflow round: a volatile/device-tape idea, and #53 (infer per-graph/routine tape write-set/footprint for static checks). Other open: #32 dispatch-trampoline threading, #51 prong 2 (LSP4IJ SemanticTokensColorsProvider), #29; upstream watch: redhat-developer/lsp4ij#1612.
